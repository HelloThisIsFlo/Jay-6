---
phase: 02.1-visual-redesign-adoption
plan: 01
subsystem: ui
tags: [svelte, css-tokens, visual-redesign, app-shell, vitest]

requires:
  - phase: 02-post-prototype-polish-uat-acceptance
    provides: v1 instrument behavior, keyboard handling, App.svelte bridge, iPad scroll escape
provides:
  - Shared sketch-derived CSS token layer
  - Single global token import before App mount
  - Token-based app shell and quiet footer styling
  - Baseline verification record before visual edits
affects: [02.1-02-topbar, 02.1-03-pads, 02.1-04-browser-smoke]

tech-stack:
  added: []
  patterns:
    - Small shared CSS token file plus scoped Svelte component CSS
    - Vitest source contract for CSS token values and import order

key-files:
  created:
    - src/styles/tokens.css
    - test/tokens.test.ts
    - test/node-shims.d.ts
  modified:
    - src/main.ts
    - src/App.svelte
    - tsconfig.json

key-decisions:
  - "Use a small shared CSS token layer instead of a component library or larger design-system extraction."
  - "Keep App.svelte behavior untouched and limit shell work to markup/CSS styling."
  - "Use narrow test-local Node shims instead of adding @types/node or any new dependency."

patterns-established:
  - "Shared tokens are imported once from src/main.ts before App mounts."
  - "Orange token comments lock --accent to active pad sound only; --system covers secondary status."
  - "App shell styling reads tokens through var(...) while the behavior bridge remains in App.svelte."

requirements-completed:
  - R1-visual-token-adoption
  - R5-responsive-redesign-coverage
  - R6-existing-behavior-preservation

coverage:
  - id: D1
    description: "Shared CSS token layer exposes the approved sketch palette, typography, compact radii, and spacing values, and is imported once before App mounts."
    requirement: R1-visual-token-adoption
    verification:
      - kind: unit
        ref: "test/tokens.test.ts#shared visual tokens"
        status: pass
      - kind: other
        ref: "just check"
        status: pass
      - kind: other
        ref: "just test"
        status: pass
    human_judgment: false
  - id: D2
    description: "App.svelte shell and footer now use the shared token direction while preserving keyboard, highlight projection, EngineHost, and tickSource bridges."
    requirement: R6-existing-behavior-preservation
    verification:
      - kind: other
        ref: "just ci"
        status: pass
    human_judgment: true
    rationale: "Visual fit and responsive reachability are browser-judgment checks owned by the phase browser-smoke plan."

duration: 5min
completed: 2026-07-06
status: complete
---

# Phase 02.1 Plan 01: Shared Tokens and App Shell Summary

**Shared Jay-6 v3 visual tokens and token-based app shell chrome without changing the v1 instrument bridge.**

## Performance

- **Duration:** 5min
- **Started:** 2026-07-06T14:43:57Z
- **Completed:** 2026-07-06T14:48:18Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Captured a green Wave 0 baseline before source edits: `just test`, `just check`, and `just ci`.
- Added `src/styles/tokens.css` with the approved dark chrome, cream pad, active orange, steel system, mono typography, compact radii, and spacing tokens.
- Imported the token layer exactly once from `src/main.ts` before `App.svelte` mounts.
- Restyled `App.svelte` root shell, footer, shortcut chips, and dev tag to use shared tokens while preserving all behavior anchors.

## Task Commits

1. **Task 0: Capture the Wave 0 verification baseline** - `5ae3ce5` (chore)
2. **Task 1 RED: Shared token contract test** - `61b7872` (test)
3. **Task 1 GREEN: Shared token layer and import** - `d4a17ff` (feat)
4. **Task 2: Restyle the app shell around the instrument surface** - `3247785` (feat)

## Files Created/Modified

- `src/styles/tokens.css` - Shared CSS custom properties for sketch palette, typography, radii, spacing, and semantic accents.
- `src/main.ts` - Imports shared tokens once before mounting the app.
- `src/App.svelte` - Uses tokenized app background, footer, shortcut chip, and dev-tag styling.
- `test/tokens.test.ts` - Verifies required token values and import order.
- `test/node-shims.d.ts` - Narrow test-local declarations for source-file reads without a dependency install.
- `tsconfig.json` - Includes test declaration files for `svelte-check`.

## Verification

- **Baseline before edits:** `just test` pass, `just check` pass, `just ci` pass.
- **Task 1 RED:** `just test` failed as expected before implementation because token CSS/import were absent.
- **Task 1 GREEN:** `just check` pass; `just test` pass with 47 tests.
- **Task 2:** `just check` pass; `just ci` pass with 47 tests and production build.
- **Acceptance greps:** Confirmed `host.setStyle(ui.style, ui.variation)`, `tickSource.setMode(ui.clockSource)`, `heldKeys={displayKeys}`, `onKeyDown`, `onKeyUp`, and `max-height: 480px` remain present.

## Decisions Made

- Used the sketch token file as a small shared CSS layer, matching D-01 and D-02 without adding any UI library.
- Kept App behavior intact by limiting Task 2 to CSS/markup around the existing component order.
- Added Vitest coverage for token values because Task 1 was TDD and the token contract is source-verifiable.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added test-local Node shims for `svelte-check`**
- **Found during:** Task 1
- **Issue:** The RED test needed to read CSS source, but the project intentionally lacks `@types/node`; `svelte-check` rejected `node:fs`, `node:path`, and `__dirname`.
- **Fix:** Added narrow declarations in `test/node-shims.d.ts` and included `test/**/*.d.ts` in `tsconfig.json`.
- **Files modified:** `test/node-shims.d.ts`, `tsconfig.json`, `test/tokens.test.ts`
- **Verification:** `just check` and `just test` pass.
- **Committed in:** `d4a17ff`

**Total deviations:** 1 auto-fixed Rule 3 issue.
**Impact on plan:** No runtime or dependency impact; the fix is test-only type support needed for the TDD gate.

## Issues Encountered

- Vite CSS `?raw` / `?inline` imports returned an empty stylesheet string under the current Vitest setup, so the token test reads the source file instead.

## Known Stubs

None.

## Threat Flags

None.

## Authentication Gates

None.

## TDD Gate Compliance

- RED commit present: `61b7872`.
- GREEN commit present after RED: `d4a17ff`.
- Refactor commit not needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Wave 2 plans can rely on `src/styles/tokens.css` and the global import being present.
- TopBar and PianoLayout redesign work can use the shared token names immediately.
- Browser visual smoke remains owned by Plan 04, as planned.

## Self-Check: PASSED

- `src/styles/tokens.css` exists.
- `.planning/phases/02.1-visual-redesign-adoption/02.1-01-SUMMARY.md` exists.
- Task commits found: `5ae3ce5`, `61b7872`, `d4a17ff`, `3247785`.
- No generated untracked files remain from verification.

---
*Phase: 02.1-visual-redesign-adoption*
*Completed: 2026-07-06*
