---
phase: 02-post-prototype-polish-uat-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REQ-clock-receive
  - REQ-deploy-cloudflare-dev
  - REQ-deploy-k8s-always-on
  - REQ-lan-exposure
  - REQ-ipad-web-midi-browser
must_haves:
  truths:
    - "All five carry-forward REQs from pre-GSD prototype work have a recorded acceptance criterion."
    - "https://jay-6.kempenich.ai returns HTTP 200 from the always-on K8s deployment."
    - "Cloudflare tunnel recipe `just serve` exists in justfile."
    - "LAN exposure recipe `just dev-lan` exists in justfile."
  artifacts:
    - path: ".planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md"
      provides: "Acceptance criteria record for the five shipped pre-GSD requirements."
  key_links:
    - from: ".planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md"
      to: ".planning/REQUIREMENTS.md"
      via: "REQ IDs cross-referenced in section headers"
      pattern: "REQ-(clock-receive|deploy-cloudflare-dev|deploy-k8s-always-on|lan-exposure|ipad-web-midi-browser)"
---

<objective>
Record acceptance criteria for the five Phase 2 requirements that shipped before GSD bootstrap, so every REQ-ID in `REQUIREMENTS.md` appears in a plan and has a one-line "still satisfied" check.

Purpose: Satisfies the "every REQ must appear in a plan" rule for the five carry-forward requirements (REQ-clock-receive, REQ-deploy-cloudflare-dev, REQ-deploy-k8s-always-on, REQ-lan-exposure, REQ-ipad-web-midi-browser) without inventing fresh code work for already-shipped behavior.

Output: A single `02-01-CARRY-FORWARD.md` record under the phase directory + a `curl` check confirming the always-on deploy is live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
@justfile
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 02-01-CARRY-FORWARD.md acknowledgement record</name>
  <files>.planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    justfile
    README.md
  </read_first>
  <action>
    Create `02-01-CARRY-FORWARD.md` with one section per REQ. Each section has: REQ-ID heading, one-sentence summary lifted from REQUIREMENTS.md, and a single bulleted "Acceptance check" line stating exactly how to verify the carry-forward is still satisfied. Use these acceptance checks:
    - REQ-clock-receive: "Open the app in Chrome, select a MIDI input port in the TopBar Input dropdown, flip Clock to Ext, press play on the external clock source — BPM field disables and rhythm engines slave to incoming clock."
    - REQ-deploy-cloudflare-dev: "Run `just serve`; tunnel URL `https://jay-6.kempenich.dev` returns 200 and the app loads."
    - REQ-deploy-k8s-always-on: "`curl -sSI https://jay-6.kempenich.ai` returns `HTTP/2 200` (the K8s always-on deploy)."
    - REQ-lan-exposure: "`just dev-lan` exists in justfile and binds Vite to `0.0.0.0` so an iPad on the same LAN can reach the app (MIDI denied over plain http — expected per Web MIDI secure-context rule)."
    - REQ-ipad-web-midi-browser: "On iPad inside Yonemoto's Web MIDI Browser app, navigate to `https://jay-6.kempenich.dev`, MIDI permission prompt appears, OP-1 appears in Output dropdown, chord pads play."
    Add a final note: "These requirements shipped before GSD bootstrap (see commits b73dc98 + d5b8fb2). This file records the acceptance bar without re-implementing — D-15 places hands-on re-verification inside the Phase 2 UAT walkthrough (verify-phase)."
  </action>
  <verify>
    <automated>test -f .planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md && grep -c "^## REQ-" .planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md | grep -q "^5$"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md` exists.
    - File contains exactly five `## REQ-` headings, one per carry-forward requirement.
    - Each section has an "Acceptance check" bullet that is verifiable by a human in one step (URL visit, CLI command, or single UI action).
  </acceptance_criteria>
  <done>The five carry-forward REQs are formally documented with acceptance checks so REQUIREMENTS.md coverage is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm always-on K8s deploy is live</name>
  <files></files>
  <read_first>
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-01-CARRY-FORWARD.md
  </read_first>
  <action>
    Run a single `curl -sSI https://jay-6.kempenich.ai` and record the HTTP status line in the plan SUMMARY. If status is not 200 (transient outage, cert expiry, K8s ingress issue), record the error and flag REQ-deploy-k8s-always-on as a verify-phase follow-up — do NOT attempt to fix the deploy from inside this plan (out of scope per CONTEXT.md "no infra changes"). If status is 200, mark REQ-deploy-k8s-always-on as confirmed in the SUMMARY.
  </action>
  <verify>
    <automated>curl -sSI https://jay-6.kempenich.ai | head -n 1</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sSI https://jay-6.kempenich.ai | head -n 1` returns a line starting with `HTTP/`.
    - The SUMMARY records the exact status line returned (whether 200 or otherwise).
  </acceptance_criteria>
  <done>K8s always-on deploy status is captured in the SUMMARY; REQ-deploy-k8s-always-on is either confirmed or flagged as a verify-phase follow-up with the exact failure reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan is a documentation record + a single read-only curl. No code execution, no network input parsing, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | `02-01-CARRY-FORWARD.md` | accept | File contains only public URLs and acceptance criteria — no secrets, tokens, or PII. |
| T-02-01-02 | Tampering | `curl` to `jay-6.kempenich.ai` | accept | Read-only HEAD request to a public deployment; no payload, no auth header sent. |
</threat_model>

<verification>
- `02-01-CARRY-FORWARD.md` exists with 5 REQ sections.
- `curl -sSI https://jay-6.kempenich.ai | head -n 1` was run and result captured in SUMMARY.
- `just test` still passes (no source changes).
</verification>

<success_criteria>
Five carry-forward REQs are documented + the live deploy status is captured. REQUIREMENTS.md coverage gap is closed.
</success_criteria>

<output>
Create `.planning/phases/02-post-prototype-polish-uat-acceptance/02-01-SUMMARY.md` when done. Include the exact HTTP status line returned by the curl check.
</output>
