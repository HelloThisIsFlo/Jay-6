---
phase: 02-post-prototype-polish-uat-acceptance
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/tickSource.ts
  - src/engines/host.ts
  - src/engines/rhythmGate.ts
  - src/engines/phraseDuration.ts
  - src/engines/arp.ts
  - src/App.svelte
  - src/state.svelte.ts
autonomous: true
requirements:
  - REQ-clock-send-transport-sync
  - REQ-rhythm-phase-alignment-ext-clock
  - REQ-voicing-second-pass-audit
must_haves:
  truths:
    - "When clock mode is Int, the selected MIDI output receives 24 PPQ clock pulses free-running from the moment Int is active (D-02)."
    - "When an engine starts (first pad press in Int mode) the selected output receives `Start`; when the engine stops it receives `Stop`."
    - "When clock mode is Ext, inbound `Start` from the MIDI input resets rhythm pattern position to 0 and arms engines to fire on the next downbeat boundary (D-04)."
    - "Inbound `Start` events arriving within 200ms of a previous `Start` are ignored (D-05 double-trigger guard, using `performance.now()`)."
    - "Inbound `Stop` from the MIDI input runs the existing `panic()` path: engine stops, latch clears, all notes off (D-04 + Pitfall 5)."
    - "Inbound `Continue` resumes the engine from its saved pattern position (D-04)."
    - "Switching the Clock toggle Int↔Ext mid-playback fires `panic()` then sets the new mode (D-03 hard stop)."
    - "Under Ext mode, `RhythmGateEngine.start()`, `PhraseDurationEngine.start()`, and `ArpEngine.start()` defer the first audible hit until the next downbeat (`tick % 24 === 0`) (D-06)."
    - "Under Int mode all three engines fire immediately on `start()` as in Phase 1 (D-07 live-feel preservation)."
    - "`test/banks.test.ts` runs with at least five reconciliation anchors passing, satisfying REQ-voicing-second-pass-audit per D-19 (anchors already shipped in commit f2d5d59 — verify-only, no additions)."
  artifacts:
    - path: "src/tickSource.ts"
      provides: "Internal-tick `sendClock` emission when mode=internal; defensive optional-chain on output."
      contains: "sendClock"
    - path: "src/engines/host.ts"
      provides: "Outbound transport sender + inbound transport handler + double-trigger guard + mode-switch hard stop + clockMode config field."
      contains: "onTransport"
    - path: "src/engines/rhythmGate.ts"
      provides: "Downbeat-aligned arm under Ext mode using `nextDownbeatTick`; Pitfall 4 `Math.floor` stepIndex fix."
      contains: "nextDownbeatTick"
    - path: "src/engines/phraseDuration.ts"
      provides: "Downbeat-aligned first fire under Ext mode."
      contains: "tickSource.getMode"
    - path: "src/engines/arp.ts"
      provides: "Downbeat-aligned first fire under Ext mode."
      contains: "tickSource.getMode"
    - path: "src/App.svelte"
      provides: "`$effect` mode-switch hard stop + `onMount` transport subscription bridge."
      contains: "subscribeTransport"
    - path: "src/state.svelte.ts"
      provides: "(no shape change; consumed for ClockSourceKind type alias)"
  key_links:
    - from: "src/App.svelte"
      to: "src/engines/host.ts"
      via: "onMount: tickSource.subscribeTransport((kind) => host.onTransport(kind))"
      pattern: "tickSource\\.subscribeTransport"
    - from: "src/engines/host.ts"
      to: "webmidi WebMidi.getOutputById"
      via: "sendTransport helper → sendStart/sendStop/sendContinue"
      pattern: "sendStart|sendStop|sendContinue"
    - from: "src/tickSource.ts"
      to: "webmidi WebMidi.getOutputById"
      via: "emitTick → output?.sendClock() when mode=internal"
      pattern: "sendClock"
    - from: "src/engines/{arp,phraseDuration,rhythmGate}.ts"
      to: "src/clock.ts"
      via: "named import of nextDownbeatTick (or TICKS_PER_QUARTER constant for countdown engines)"
      pattern: "import\\s+\\{[^}]*(nextDownbeatTick|TICKS_PER_QUARTER)[^}]*\\}\\s+from\\s+['\"]\\.\\./clock['\"]"
---

<objective>
Wire the existing primitives — `tickSource.subscribeTransport()`, `WebMidi.Output.sendClock/sendStart/sendStop/sendContinue`, `host.panic()`, the new `nextDownbeatTick` from plan 02-02 — into the working transport sync and rhythm-phase alignment behavior locked in CONTEXT.md (D-01 through D-07). Also verify the voicing-audit anchors shipped pre-phase satisfy D-19.

Purpose: Phase 2 is wiring, not building. Every primitive exists — `tickSource.subscribeTransport()` emits the right events but nothing listens; `Output.sendClock()` exists but is never called; three engines fire immediately on `start()` regardless of Ext-clock phase. This plan threads them together correctly. The risk is "did we wire it correctly," not "did we invent the right algorithm" (RESEARCH.md §Don't Hand-Roll).

Output: Transport sync (bidirectional clock + Start/Stop/Continue), rhythm phase alignment under Ext clock for all three rhythmic engines, mode-switch hard stop, double-trigger guard, and a verified voicing-anchor pass. Manual verification of MIDI behavior happens in UAT (verify-phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-VALIDATION.md
@src/tickSource.ts
@src/engines/host.ts
@src/engines/rhythmGate.ts
@src/engines/phraseDuration.ts
@src/engines/arp.ts
@src/App.svelte
@src/state.svelte.ts
@src/midi.ts
@src/clock.ts
@test/banks.test.ts

<interfaces>
Plan-relevant existing surfaces (extracted from current source — executor does not need to re-explore):

`src/tickSource.ts`:
- `export type ClockSource = 'internal' | 'external'`
- `tickSource.setMode(mode: ClockSource): void` — already hot-swaps internal timer ↔ external listener.
- `tickSource.getMode(): ClockSource`
- `tickSource.subscribe(listener: () => void): () => void` — ticks (returns unsubscribe).
- `tickSource.subscribeTransport(listener: (kind: 'start' | 'stop' | 'continue') => void): () => void` — already implemented; nothing listens yet.
- `private emitTick()` (line 111–113) — extension point for `sendClock`.

`src/midi.ts`:
- `export function getMidiState(): Readonly<State>` — provides `state.selectedOutputId`.
- `export function allNotesOff(): void` — already used by `host.panic()`.
- Defensive pattern at lines 107–112 (`getChannel`): guard-and-return-null when not ready.

`src/engines/host.ts`:
- `interface HostConfig { bpm, styleKind, variation, transpose, latch, gatePercent }` — add `clockMode: ClockSource` field per PATTERNS.md.
- `EngineHost.panic(): void` (lines 102–107) — verified all-clean path; REUSE for D-03 and inbound Stop per Pitfall 5.
- `padPressed(key, rawNotes): void` — extension point for outbound `sendStart` at the `!this.playing` branch (line 75–78).
- `padReleased(key): void` — extension point for outbound `sendStop` at the `engine.stop()` branch (line 89–91).

`src/engines/{arp,phraseDuration,rhythmGate}.ts`:
- Each implements `Engine` interface from `./types`.
- Each has `start(notes: number[]): void` that calls `tickSource.subscribe(() => this.onTick())` and then fires immediately. Phase 2 gates that immediate fire on `tickSource.getMode() === 'external'`.

`src/clock.ts`:
- `export const TICKS_PER_QUARTER = 24`
- `export function nextDownbeatTick(currentTick: number): number` — added by plan 02-02.

`src/App.svelte`:
- Existing `$effect` bridges (lines 27–32) — one-line-per-concern style. Add new effects mirroring this density.
- Existing `onMount` returns the MIDI subscribe unsubscribe (line 35) — Svelte 5 auto-cleans on unmount; mirror for `subscribeTransport`.

`src/state.svelte.ts`:
- `export type ClockSourceKind = 'internal' | 'external'` — already exists; used for the new `clockMode` field on `HostConfig`. No state shape change needed in this file — read-only consumption.

External library:
- `webmidi` v3.1.16. `WebMidi.getOutputById(id): Output | undefined`. Methods used: `output.sendClock()`, `output.sendStart()`, `output.sendStop()`, `output.sendContinue()`. All synchronous, all return `Output` for chaining, all idempotent on a single call.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Outbound clock + transport (tickSource + host send-paths)</name>
  <files>src/tickSource.ts, src/engines/host.ts, src/state.svelte.ts</files>
  <read_first>
    src/tickSource.ts
    src/engines/host.ts
    src/midi.ts
    src/state.svelte.ts
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <action>
    Two coordinated edits — outbound side only. NO inbound transport in this task (Task 2 owns that).

    A. `src/tickSource.ts` (per PATTERNS.md §"src/tickSource.ts" + RESEARCH.md Pattern 3 + Pitfall 1):
    - Add `import { getMidiState } from './midi';` next to the existing `webmidi` import on line 1.
    - Modify `private emitTick()` to first, when `this.mode === 'internal'`, look up `getMidiState().selectedOutputId`; if non-null, call `WebMidi.getOutputById(outId)?.sendClock()` (optional chain — silent no-op when output not ready, mirrors `midi.ts` lines 107–112 defensive pattern). Then run the existing `for (const l of this.listeners) l();` loop. Comment with `// D-02: clock send is always-on when Int, never when Ext (we are the slave then).`

    B. `src/engines/host.ts` (per PATTERNS.md §"src/engines/host.ts" + RESEARCH.md Pattern 4):
    - Extend the imports on line 1 to also pull `WebMidi` from `'webmidi'` and `getMidiState` from `'../midi'`. The existing `allNotesOff` import stays.
    - Add a `clockMode: ClockSource` field to `interface HostConfig` (line 10–17). Source the `ClockSource` type from `'../tickSource'`. Update the `App.svelte` instantiation in Task 3 to pass it.
    - Add private method `sendTransport(kind: 'start' | 'stop' | 'continue'): void` that early-returns when `this.cfg.clockMode !== 'internal'`, looks up the selected output id (early-return null), gets the WebMidi output, and calls `out?.sendStart()` / `out?.sendStop()` / `out?.sendContinue()` based on kind. Use the same defensive optional-chain pattern as `getChannel`. Comment with `// D-02: only master mode sends transport — slave mode listens only.`
    - In `padPressed` at the existing `if (!this.playing) { ... this.engine.start(transposed); this.playing = true; }` branch, append `this.sendTransport('start');` immediately after the `this.playing = true;` assignment. This is the first-press-fires-Start semantic per RESEARCH.md Pattern 4.
    - In `padReleased` at the existing `if (this.heldPads.size === 0) { this.engine.stop(); this.playing = false; }` branch, append `this.sendTransport('stop');` immediately after `this.playing = false;`. Last-release-fires-Stop semantic.
    - Add a public method `setClockMode(mode: ClockSource): void` that assigns `this.cfg.clockMode = mode;` — App.svelte $effect will call this in Task 3.

    Do NOT add anything for inbound transport here. Do NOT add the double-trigger guard or the downbeat alignment — Tasks 2 and 3 own those. Keep this task tightly scoped to outbound wiring to keep the diff reviewable.
  </action>
  <verify>
    <automated>grep -E "sendClock" src/tickSource.ts && grep -E "this\\.cfg\\.clockMode" src/engines/host.ts && grep -E "sendStart|sendStop|sendContinue" src/engines/host.ts && grep -E "this\\.sendTransport\\('start'\\)" src/engines/host.ts && grep -E "this\\.sendTransport\\('stop'\\)" src/engines/host.ts && grep -E "clockMode" src/engines/host.ts | grep -v "^#" | wc -l | awk '$1>=3{exit 0} {exit 1}' && just check 2>&1 | tail -10 && just test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/tickSource.ts` `emitTick()` calls `WebMidi.getOutputById(outId)?.sendClock()` only when `this.mode === 'internal'`.
    - `src/tickSource.ts` imports `getMidiState` from `'./midi'`.
    - `src/engines/host.ts` `HostConfig` interface includes a `clockMode: ClockSource` field (type imported from `'../tickSource'`).
    - `src/engines/host.ts` exports `setClockMode(mode: ClockSource): void` on `EngineHost`.
    - `src/engines/host.ts` has a `private sendTransport(kind: 'start' | 'stop' | 'continue'): void` that early-returns when `this.cfg.clockMode !== 'internal'`.
    - `src/engines/host.ts` `padPressed` calls `this.sendTransport('start')` after engine start in the `!this.playing` branch.
    - `src/engines/host.ts` `padReleased` calls `this.sendTransport('stop')` after engine stop in the `heldPads.size === 0` branch.
    - `just check` reports zero new svelte-check errors.
    - `just test` exits 0 (existing tests remain green; no test mocks WebMidi per DEC-tests-data-and-math-only).
  </acceptance_criteria>
  <done>Outbound side is wired. With this alone, switching to Int mode + pressing a pad will emit Start, free-running clock pulses, and Stop on release to the selected output. Inbound and downbeat alignment remain to ship in Tasks 2 + 3.</done>
</task>

<task type="auto">
  <name>Task 2: Inbound transport receive (host.onTransport + double-trigger guard) + engine downbeat alignment</name>
  <files>src/engines/host.ts, src/engines/rhythmGate.ts, src/engines/phraseDuration.ts, src/engines/arp.ts</files>
  <read_first>
    src/engines/host.ts
    src/engines/rhythmGate.ts
    src/engines/phraseDuration.ts
    src/engines/arp.ts
    src/clock.ts
    src/tickSource.ts
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <action>
    Two coordinated edits — inbound receive + per-engine alignment.

    A. `src/engines/host.ts` inbound transport (per PATTERNS.md §"src/engines/host.ts" + RESEARCH.md Patterns 4 + 5 + Pitfall 2 + Pitfall 5):
    - Add `private static readonly START_DEBOUNCE_MS = 200;` (D-05 guard window; single tuneable constant; mirrors `clock.ts` `TICKS_PER_QUARTER` constant-at-class-scope style).
    - Add `private lastStartMs = 0;`.
    - Add `private armedPosition: 'fresh' | 'resume' | null = null;` (D-04: armed state belongs in host, not in state.svelte.ts per RESEARCH.md Pattern 1 — it's lifecycle state, not UI state).
    - Add a public method `onTransport(kind: 'start' | 'stop' | 'continue'): void`:
      - Early-return when `this.cfg.clockMode !== 'external'` (master mode ignores inbound; D-04).
      - On `'start'`: read `performance.now()` (NOT `Date.now()` — Pitfall 2 NTP-immune). If `now - this.lastStartMs < EngineHost.START_DEBOUNCE_MS`, return (D-05 double-trigger guard). Otherwise set `this.lastStartMs = now;` and `this.armedPosition = 'fresh';`. Comment cites `D-05` and `Pitfall 2` (performance.now() not Date.now()).
      - On `'continue'`: set `this.armedPosition = 'resume';`.
      - On `'stop'`: call `this.panic();` per Pitfall 5 (reuse the verified all-clean path; don't invent a new cleanup).
    - Add a public method `panicForModeSwitch(): void` that simply calls `this.panic();` (alias for clarity at the App.svelte call site per PATTERNS.md `host.ts` example; D-03 mode-switch hard stop).

    B. `src/engines/rhythmGate.ts` downbeat alignment (per PATTERNS.md §"src/engines/rhythmGate.ts" + RESEARCH.md Pattern 2 + Pitfall 4):
    - Extend the existing import on line 2 from `import { ticksPerSixteenth } from '../clock';` to also import `nextDownbeatTick`.
    - Add a new field `private armUntilTick: number | null = null;` near the existing `tickCount` field (~line 22).
    - Modify `start(notes)` (line 42–49). After the existing `this.unsubscribe = tickSource.subscribe(...)` and the `if (notes.length === 0) return;`, branch on `tickSource.getMode()`: if `'external'`, set `this.armUntilTick = nextDownbeatTick(this.tickCount);` (D-06 — at `tickCount=0` returns 24). Else (`'internal'`), call `this.evaluateStep();` as today (D-07 — live-feel preserved).
    - Modify `onTick()` (line 71). At the top, branch on `this.armUntilTick !== null`: if `this.tickCount < this.armUntilTick`, increment `this.tickCount += 1` and return (still arming, no audio). If `this.tickCount === this.armUntilTick`, clear `this.armUntilTick = null;` and call `this.evaluateStep();` then return (first audible hit on the downbeat). Otherwise fall through to the existing onTick logic unchanged.
    - Pitfall 4 fix on the same line edit pass: change line 89's `const stepIndex = (this.tickCount / this.ticksPerStep) % 16;` to `const stepIndex = Math.floor(this.tickCount / this.ticksPerStep) % 16;` (defensive against dropped ticks under external clock; CONCERNS.md callout).

    C. `src/engines/phraseDuration.ts` downbeat alignment (per PATTERNS.md §"src/engines/phraseDuration.ts" + RESEARCH.md Pattern 2 — countdown variant):
    - Extend imports to add `TICKS_PER_QUARTER` from `'../clock'`.
    - Modify `start(notes)` (line 26–33). Set `this.ticksUntilNext = tickSource.getMode() === 'external' ? TICKS_PER_QUARTER : this.ticksPerStep;` (D-06: Ext waits 24 ticks for the next quarter; D-07: Int uses normal step length so the existing `this.fire()` call lands immediately as today). Gate the existing `this.fire();` call with `if (tickSource.getMode() === 'internal') this.fire();` so Ext mode defers first audible hit to the countdown.

    D. `src/engines/arp.ts` downbeat alignment (per PATTERNS.md §"src/engines/arp.ts" — same countdown variant as phraseDuration):
    - Extend imports to add `TICKS_PER_QUARTER` from `'../clock'`.
    - Modify `start(notes)` (line 52–61). Set `this.ticksUntilNext = tickSource.getMode() === 'external' ? TICKS_PER_QUARTER : this.ticksPerStep;`. Gate the existing `this.fireNext();` with `if (tickSource.getMode() === 'internal') this.fireNext();`.

    All three engines: WHY-only comment citing the decision ID (`D-06`, `D-07`) on each branch — see existing `host.ts` line 67–69 "Roland J-6 HOLD convention" style.

    Do NOT introduce a separate `transport.ts` module (RESEARCH.md anti-pattern — too small to justify a module). Do NOT pass `tickCount` into engines as a constructor arg (anti-pattern — engines own their tickCount). Do NOT mock `WebMidi` for tests (DEC-tests-data-and-math-only).
  </action>
  <verify>
    <automated>grep -E "onTransport\\(kind: 'start' \\| 'stop' \\| 'continue'\\)" src/engines/host.ts && grep -E "START_DEBOUNCE_MS = 200" src/engines/host.ts && grep -E "performance\\.now\\(\\)" src/engines/host.ts && grep -E "panicForModeSwitch" src/engines/host.ts && grep -E "armUntilTick" src/engines/rhythmGate.ts && grep -E "nextDownbeatTick" src/engines/rhythmGate.ts && grep -E "Math\\.floor\\(this\\.tickCount / this\\.ticksPerStep\\) % 16" src/engines/rhythmGate.ts && grep -E "tickSource\\.getMode\\(\\) === 'external'" src/engines/phraseDuration.ts && grep -E "tickSource\\.getMode\\(\\) === 'external'" src/engines/arp.ts && grep -E "TICKS_PER_QUARTER" src/engines/phraseDuration.ts && grep -E "TICKS_PER_QUARTER" src/engines/arp.ts && just ci 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/host.ts` declares `private static readonly START_DEBOUNCE_MS = 200`.
    - `src/engines/host.ts` `onTransport` uses `performance.now()` (regex match confirms — Pitfall 2 prevents Date.now()).
    - `src/engines/host.ts` `onTransport('stop')` branch calls `this.panic()` (Pitfall 5 — reuse verified path).
    - `src/engines/host.ts` exports `panicForModeSwitch(): void` method.
    - `src/engines/rhythmGate.ts` has `armUntilTick: number | null` field and uses `nextDownbeatTick` from `../clock`.
    - `src/engines/rhythmGate.ts` `evaluateStep` line uses `Math.floor(this.tickCount / this.ticksPerStep) % 16` (Pitfall 4).
    - `src/engines/phraseDuration.ts` and `src/engines/arp.ts` both check `tickSource.getMode() === 'external'` in their `start()` methods and import `TICKS_PER_QUARTER` from `'../clock'`.
    - `just ci` exits 0 (svelte-check + vitest + vite build all green).
  </acceptance_criteria>
  <done>Inbound transport receive + rhythm phase alignment ship together. Functional verification (real OP-1 + browser) is deferred to UAT §15 per 02-VALIDATION.md manual-only table.</done>
</task>

<task type="auto">
  <name>Task 3: App.svelte wiring + voicing-anchor verification</name>
  <files>src/App.svelte, test/banks.test.ts</files>
  <read_first>
    src/App.svelte
    src/engines/host.ts
    src/tickSource.ts
    src/state.svelte.ts
    test/banks.test.ts
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
  </read_first>
  <action>
    Two unrelated-but-cheap edits bundled because they're small and independent.

    A. `src/App.svelte` transport wiring (per PATTERNS.md §"src/App.svelte" + RESEARCH.md Pattern 1):
    - In the `EngineHost` constructor call (lines 17–24) add `clockMode: ui.clockSource,` as a new field alongside the existing six (mirrors the existing `bpm: ui.bpm` density).
    - Locate the existing `$effect(() => { tickSource.setMode(ui.clockSource); });` on line 32. REPLACE that single-line effect with a three-line body inside the same `$effect`:
      1. `host.panicForModeSwitch();` (D-03 hard stop BEFORE the mode flip; uses the verified panic path)
      2. `host.setClockMode(ui.clockSource);` (tell host who the master is so `sendTransport` gating works)
      3. `tickSource.setMode(ui.clockSource);` (existing line, unchanged)
      Comment the block with `// D-03: mode switch is a hard stop (panic + clear latch) before flipping tickSource. host.setClockMode() gates outbound transport per D-02.`
    - Add a new `onMount` block (or extend the existing one on line 35 to also subscribe) that calls `tickSource.subscribeTransport((kind) => host.onTransport(kind))` and returns the unsubscribe. PATTERNS.md shows two acceptable shapes — pick the one that keeps the existing `onMount(() => subscribeMidi(...))` line untouched (add a second `onMount`); Svelte 5 supports multiple `onMount` calls and auto-cleans returned unsubscribes. Comment with `// D-04: forward inbound transport to host; host decides arm vs resume vs stop, with D-05 double-trigger guard.`
    - Do NOT modify `<style>` block — plan 02-03 (Wave 1) owns that. Do NOT add new state to `state.svelte.ts` for the armed position — it's engine-lifecycle state (lives on host) per RESEARCH.md Pattern 1.

    B. `test/banks.test.ts` voicing-anchor verification (per RESEARCH.md Open Question Q1 + D-19):
    - Read the current file. The reconciliation anchors from commit f2d5d59 should already be present (bank 1/A FM/A, bank 30/A G/B, bank 46/A Em, bank 79/A F7/A, bank 95/C FM7/E) alongside the 2 original anchors (bank 1/C Cadd9, bank 14/C Oct Stack). That is 7 anchors total. D-19 spec calls for 3–5 reconciled anchors (planner reads this as "already satisfied at 5 reconciled + 2 original").
    - Action: do NOT add additional anchor tests. Run `just test 2>&1 | grep -E "(banks data|✓|×|FAIL|passed|failed)"` and confirm all 7 anchor cases pass and the file shows at least 5 reconciliation anchor assertions (greppable: `expect.*notes\\)\\.toEqual\\(\\[`).
    - If the file does NOT contain at least 5 reconciliation-anchor it-cases (i.e., the commit isn't actually present), add the missing ones using the exact data from `02-PATTERNS.md` §"`test/banks.test.ts`" table. Use the existing test-shape pattern at lines 51–55 of the same file (find chord via `bank.chords.find((c) => c.key === 'A')!`, assert `c.name` and `c.notes`).
    - This satisfies REQ-voicing-second-pass-audit per D-18 ("planner picks up only the commit + a UAT spot-check"). Final ear/manual spot-check happens in UAT (verify-phase).
  </action>
  <verify>
    <automated>grep -E "tickSource\\.subscribeTransport" src/App.svelte && grep -E "host\\.panicForModeSwitch" src/App.svelte && grep -E "host\\.setClockMode" src/App.svelte && grep -E "clockMode: ui\\.clockSource" src/App.svelte && grep -cE "expect\\(c\\.notes\\)\\.toEqual\\(\\[" test/banks.test.ts | awk '$1>=5{exit 0} {exit 1}' && just ci 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.svelte` `EngineHost` constructor includes `clockMode: ui.clockSource` field.
    - `src/App.svelte` has an `$effect` block that calls in order: `host.panicForModeSwitch()`, `host.setClockMode(ui.clockSource)`, `tickSource.setMode(ui.clockSource)`.
    - `src/App.svelte` has a `tickSource.subscribeTransport((kind) => host.onTransport(kind))` call inside an `onMount`.
    - `test/banks.test.ts` contains at least 5 lines matching `expect(c.notes).toEqual([...` (the reconciliation-anchor assertion shape).
    - `just ci` exits 0 (svelte-check + vitest + vite build all green).
  </acceptance_criteria>
  <done>App.svelte bridges the transport contract end-to-end (mode-switch hard stop + onMount transport subscribe). Voicing audit is formally satisfied with passing anchors. The entire Phase 2 wiring is in place; final hardware verification is the UAT walkthrough (handled by plan 02-05 + verify-phase).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External MIDI device → `tickSource` listeners | OP-1 (or any device) sends clock/start/stop/continue bytes. Already validated by WEBMIDI.js v3 (typed events; no raw byte parsing in app code). |
| `tickSource` → `host.onTransport` | In-process function call; transport `kind` constrained to TypeScript union `'start' \| 'stop' \| 'continue'` at the public method signature. |
| `host` → `WebMidi.Output.send*` methods | In-process; WEBMIDI.js encodes outbound transport bytes per MIDI spec — no manual byte-array construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering (of musical state via double-fired Start) | `host.onTransport('start')` | mitigate | D-05 200ms debounce window using `performance.now()` (NOT `Date.now()` per Pitfall 2 — NTP-immune monotonic clock). Window is a tuneable constant; over-aggressive case surfaces in UAT §15 as "legit Start ignored", over-loose case as "double-trigger reappears". |
| T-02-04-02 | Tampering (of musical state via stuck notes after mode switch) | `App.svelte` `$effect` mode-switch path | mitigate | D-03 calls `host.panicForModeSwitch()` (alias for `panic()`) BEFORE `tickSource.setMode()` — reuses the verified all-clean path per Pitfall 5. Avoids the "engine.stop() runs twice → null deref" failure mode. |
| T-02-04-03 | Denial of Service (clock feedback loop in Ext mode) | `tickSource.emitTick` `sendClock` gating | mitigate | The `if (this.mode === 'internal')` guard in `emitTick` is the only thing preventing Jay-6 from echoing OP-1's clock back to the same wire. Pitfall 8 documents this; UAT §15 gets a new checklist item (Wave 0 in 02-VALIDATION.md): "Ext mode does not send clock pulses downstream." |
| T-02-04-04 | Tampering (of pattern alignment via fractional stepIndex) | `RhythmGateEngine.evaluateStep` line 89 | mitigate | Replace `(tickCount / ticksPerStep) % 16` with `Math.floor(tickCount / ticksPerStep) % 16`. Defensive against dropped ticks under external clock — CONCERNS.md callout + Pitfall 4. |
| T-02-04-05 | DoS (silent failure when output unselected) | `tickSource.sendClock` + `host.sendTransport` | mitigate | Both use optional-chain (`out?.sendClock()`) and early-return when `selectedOutputId` is null. Mirrors `midi.ts` `getChannel` defensive pattern. Failure mode is silent no-op (correct behavior), not thrown error. |
| T-02-04-06 | Information Disclosure | MIDI transport messages | accept | MIDI messages contain no PII; transport bytes (`0xFA`/`0xFB`/`0xFC`) are public protocol. No exposure increase. |
| T-02-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs zero new packages (RESEARCH.md §Package Legitimacy Audit explicitly skipped). No supply-chain risk introduced. |
</threat_model>

<verification>
- `just ci` exits 0 (svelte-check + vitest + vite build).
- `just test` shows `nextDownbeatTick` describe block green AND all 7 banks anchors green.
- Grep gates in each task's verify command pass.
- Hardware/audible verification (REQ-clock-send-transport-sync + REQ-rhythm-phase-alignment-ext-clock + REQ-voicing-second-pass-audit ear spot-check) is deferred to UAT in verify-phase per DEC-tests-data-and-math-only and per 02-VALIDATION.md Manual-Only Verifications table.
</verification>

<success_criteria>
Transport sync wiring, rhythm phase alignment, double-trigger guard, mode-switch hard stop, and voicing-anchor coverage are all in place. The UAT walkthrough (plan 02-05 + verify-phase) is the final hardware-loop gate for v1 close.
</success_criteria>

<output>
Create `.planning/phases/02-post-prototype-polish-uat-acceptance/02-04-SUMMARY.md` when done. Record: total svelte-check + vitest pass count, exact files touched, the line count delta per file (to confirm changes stayed small per the "Phase 2 is wiring, not building" thesis), and the UAT sections that will exercise the new behavior (15 — Clock + transport sync; 19 — edge cases).
</output>
