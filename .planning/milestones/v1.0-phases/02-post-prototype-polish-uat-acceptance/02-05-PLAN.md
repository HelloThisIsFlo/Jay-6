---
phase: 02-post-prototype-polish-uat-acceptance
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - MANUAL.md
  - README.md
  - CURRENT-STATE.md
autonomous: true
requirements:
  - REQ-user-manual
  - REQ-uat-walkthrough
  - REQ-edge-cases
  - REQ-gate-slider
must_haves:
  truths:
    - "`MANUAL.md` exists at repo root and renders correctly on GitHub."
    - "`MANUAL.md` contains the four required D-14 sections in order: Setup, Pads + chords, Styles, Clock + transport sync."
    - "`MANUAL.md` is tone-aligned with Teenage Engineering Pocket Operator manuals (consumer-product voice — not internals-leaking like Roland's J-6 manual)."
    - "`MANUAL.md` is structured to grow — adding a Section 5 for v2 sequencer requires no rewrite of Sections 1–4 (D-13)."
    - "`README.md` 'More docs' section links to `MANUAL.md`."
    - "`CURRENT-STATE.md` Reference section links to `MANUAL.md`."
    - "REQ-uat-walkthrough is documented as the verify-phase gate via the `.claude/skills/uat-agent` skill (`say 'run uat'` triggers it); REQ-edge-cases (UAT §19) and REQ-gate-slider (UAT §12, flagged suspect) are explicit checklist items inside `.research/UAT.md`."
  artifacts:
    - path: "MANUAL.md"
      provides: "Consumer-product user manual at repo root."
      min_lines: 80
      contains: "# Setup"
    - path: "README.md"
      provides: "Updated 'More docs' section linking MANUAL.md."
      contains: "MANUAL.md"
    - path: "CURRENT-STATE.md"
      provides: "Updated Reference section linking MANUAL.md."
      contains: "MANUAL.md"
  key_links:
    - from: "README.md"
      to: "MANUAL.md"
      via: "Markdown link in 'More docs' section"
      pattern: "\\[`MANUAL\\.md`\\]\\(MANUAL\\.md\\)"
    - from: "CURRENT-STATE.md"
      to: "MANUAL.md"
      via: "Markdown link in Reference section"
      pattern: "MANUAL\\.md"
---

<objective>
Author `MANUAL.md` at the repo root per D-11..D-14 (the new Phase 2 deliverable that defines "shipped"), link it from `README.md` and `CURRENT-STATE.md`, and formally register REQ-uat-walkthrough + REQ-edge-cases + REQ-gate-slider as verify-phase gates handled by the existing `.claude/skills/uat-agent` skill.

Purpose: Phase is "shipped" when a non-source-spelunking user can pick up Jay-6 and use it from MANUAL.md alone (D-11). The four required D-14 sections (Setup, Pads + chords, Styles, Clock + transport sync) cover the entire v1 surface — and Section 4 documents the brand-new clock + transport sync behavior shipped by plan 02-04. The manual structurally accommodates a future Section 5 for v2 sequencer without touching Sections 1–4 (D-13).

Output: One new file (`MANUAL.md`), two one-line additions (`README.md`, `CURRENT-STATE.md`), and a SUMMARY that hands off REQ-uat-walkthrough to verify-phase with `uat-agent`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
@README.md
@CURRENT-STATE.md
@.claude/skills/uat-agent/SKILL.md
@.research/UAT.md
@src/state.svelte.ts
@src/components/TopBar.svelte

<interfaces>
Tone anchors and structural patterns the writer needs:

- D-13 (locked): consumer-product voice. Aesthetic anchor = Teenage Engineering Pocket Operator manuals (compact, friendly, function-first; one screen per concept; no "Tip:" / "Note:" callout boxes — caveats inline). Anti-reference = Roland J-6 manual itself (terse, internals-leaking, weak structure).
- D-14 (locked): required sections in order: Setup → Pads + chords → Styles → Clock + transport sync.
- README.md style (closest in-repo anchor): emoji on H2 headers only; sub-bullets stay clean; tables for matrices (browser support, keyboard map, URL list); short prose; no fluff. See README.md lines 27–36 (keyboard table) and lines 41–51 (browser-support table + secure-context caveat inline).
- URL matrix to lift verbatim from README.md lines 41–51 (browser support) + lines 19–24 (quick-start). Local-MIDI caveat: `jay-6.kempenich.ai` is always-on but Web MIDI is local-only, so it loads everywhere but only drives MIDI on the machine running the browser.
- Keyboard map source-of-truth: `src/App.svelte` `KEY_TO_PAD` (lines 66–69): whites = `a s d f g h j` → `C D E F G A B`; blacks = `w e t y u` → `C# D# F# G# A#`. Plus `z`/`x` transpose, `←`/`→` bank, `Space` latch, `1`–`6` style.
- Style list source-of-truth: `src/state.svelte.ts` `STYLE_LABELS` (lines 11–18): Hold · Arp (Style 1, 8th) · Arp (Style 2, 16th) · Phrase Dur (Style 3) · Rhythm Gate (Style 4) · Rhythm Gate (Style 5). 12 variations each except Hold (0).
- Clock + transport semantics for Section 4 (lifted from CONTEXT.md D-01..D-07 and the behavior shipped by plan 02-04):
  - Int = Jay-6 is master; sends 24 PPQ clock free-running + Start on first pad press + Stop on last release.
  - Ext = Jay-6 is slave; BPM input disables; rhythm engines wait for the next downbeat after Start before firing first hit; OP-1 Record is treated identically to Start; 200ms double-trigger guard.
  - Mode switch Int↔Ext mid-play = hard stop (panic + clear latch).
- iPad workflow: Yonemoto's "Web MIDI Browser" app on iOS, point at `https://jay-6.kempenich.dev` (Cloudflare tunnel from Flo's Mac), OP-1 plugged into iPad via camera-kit appears in Output dropdown.

UAT handoff:
- `.claude/skills/uat-agent/SKILL.md` exists; trigger phrase "run uat" walks `.research/UAT.md` interactively, one section at a time, updating checkboxes in place. REQ-edge-cases is UAT §19; REQ-gate-slider is UAT §12 (flagged ⚠️ suspect — bring MIDI monitor per CONTEXT.md "blockers/concerns"). REQ-uat-walkthrough = the whole walkthrough as the v1 close gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author MANUAL.md at repo root</name>
  <files>MANUAL.md</files>
  <read_first>
    README.md
    CURRENT-STATE.md
    src/App.svelte
    src/components/TopBar.svelte
    src/state.svelte.ts
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <action>
    Create `MANUAL.md` at the repo root. Open with a one-line top blurb ("Browser-based Roland J-6 — chord pads, 5 playback styles, MIDI out to your gear.") and a short TOC linking the four sections. Then ship the four required D-14 sections in order, each an H1 (`#`) per D-14 ("Sections" wording). Section bodies:

    1. `# Setup` (short — keep under 25 lines): browser requirement (Chrome / Edge desktop; iPad needs "Web MIDI Browser" app — lift the table from README.md lines 41–49 with the Web MIDI secure-context caveat inline, no callout box per the anti-pattern); URL matrix (localhost via `just dev`, Cloudflare tunnel `https://jay-6.kempenich.dev` via `just serve`, always-on `https://jay-6.kempenich.ai` — note local-MIDI caveat inline per Open Question Q3 of RESEARCH.md); MIDI permission prompt flow (load the app → browser asks → grant → ports appear); picking Output + Input + Channel from the TopBar.

    2. `# Pads + chords`: 100 banks (cycle with `‹ ›` arrows, `← →` keys, or the dropdown); 12 chords per bank in J-6 layout (5 black on top, 7 white on bottom); press a pad = play, release = stop; transpose with `±12` buttons or `Z`/`X` (clamped at `±36`); latch with the button OR `Space` (latched pads stay orange, same-pad re-press retriggers per Roland's HOLD convention, different pad swaps the chord smoothly); Ableton-style keyboard map as a table lifted from README.md lines 27–36.

    3. `# Styles`: brief paragraph per style explaining sound + when to reach for it. Use the exact labels from `src/state.svelte.ts` `STYLE_LABELS`. Hold (no clock — just play the chord); Arp (Style 1, 8th) and Arp (Style 2, 16th) with 12 variations each (direction × octave range × triplet flag); Phrase Dur (Style 3) with 12 variations (whole down to 16th + triplets); Rhythm Gate (Style 4 + Style 5) with 12 patterns each + the Gate slider (10% staccato → 100% legato).

    4. `# Clock + transport sync`: this section documents the Phase 2 wiring (must wait for plan 02-04 to ship — dependency). Int vs Ext (Int = Jay-6 master, sends 24 PPQ + Start/Stop; Ext = Jay-6 slave, BPM disables, OP-1 drives tempo); chaining OP-1 as master or slave (table: Jay-6 mode | what OP-1 controls | what Jay-6 sends/receives); rhythm engines under Ext wait for the next downbeat before firing first hit (musical reason: keeps you locked to the external grid); OP-1 Record is treated as Start; switching Int↔Ext mid-play stops everything (clean restart by design — D-03); iPad workflow paragraph (Web MIDI Browser app + camera-kit cable to OP-1; one-line caveat that desktop machines on the .ai URL can't drive MIDI on a different physical OP-1).

    Tone rules: bullets over prose; tables for matrices; emoji on H1/H2 only (sub-bullets stay clean per global style and README.md analog); no "Tip:" / "Note:" boxes — write caveats inline (README.md line 51 is the established analog); code fences only for shell commands; one-screen-per-concept density (Pocket Operator vibe). End with a `# What's next` H1 that's a single sentence: "Sequencer coming in v2 — section 5 of this manual will document it without changing 1–4" (this is the D-13 "designed to grow" hook the executor of future v2 work needs).

    Do NOT include: internals (how the engine host works, the tickSource, the panic state machine); a "Troubleshooting" section (out of v1 scope — defer to a Phase 3+ pass); the existing in-app help overlay (`?` button) — that idea is deferred per CONTEXT.md.
  </action>
  <verify>
    <automated>test -f MANUAL.md && grep -E "^# Setup" MANUAL.md && grep -E "^# Pads \\+ chords" MANUAL.md && grep -E "^# Styles" MANUAL.md && grep -E "^# Clock \\+ transport sync" MANUAL.md && grep -c "^# " MANUAL.md | awk '$1>=4{exit 0} {exit 1}' && wc -l MANUAL.md | awk '$1>=80{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `MANUAL.md` exists at the repo root.
    - File contains exactly four required H1 sections in order: `# Setup`, `# Pads + chords`, `# Styles`, `# Clock + transport sync` (verifiable by grep).
    - File length is ≥80 lines (substantive coverage of all four sections — not a stub).
    - File contains no `> **Note:**` / `> **Tip:**` callout-box patterns (matches D-13 + analog anti-pattern grep: zero matches expected for `^> \\*\\*(Note|Tip|Warning):`).
    - File contains the URL `https://jay-6.kempenich.dev` AND `https://jay-6.kempenich.ai` (URL matrix present per Open Question Q3).
    - File contains the keyboard map table (greppable: at least one row with `A S D F G H J` and the C..B key labels).
    - File documents both `Int` and `Ext` clock modes in the clock section.
  </acceptance_criteria>
  <done>MANUAL.md ships at repo root, structurally and tonally aligned with D-11..D-14. A first-time user can play Jay-6 from the manual alone without reading source code.</done>
</task>

<task type="auto">
  <name>Task 2: Link MANUAL.md from README.md and CURRENT-STATE.md</name>
  <files>README.md, CURRENT-STATE.md</files>
  <read_first>
    README.md
    CURRENT-STATE.md
    MANUAL.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <action>
    Two one-line additions per D-12 ("Linked from README.md + CURRENT-STATE.md").

    A. `README.md`: Locate the existing `## 📚 More docs` section (line 53). Add a new bullet as the FIRST item in that list (so MANUAL.md is most-prominent for first-time readers): `- [`MANUAL.md`](MANUAL.md) — user guide (how to play, clock sync, iPad setup)`. Preserve the existing two bullets (`CURRENT-STATE.md` + `.research/PLAN.md`) below it, order unchanged.

    B. `CURRENT-STATE.md`: Open the file, find a sensible "Reference" or "Start here" section (the file has a "Reference" block per CONTEXT.md interfaces — confirm by reading). Add a single bullet pointing to MANUAL.md: `- User manual: [`MANUAL.md`](MANUAL.md)`. Place it adjacent to other top-level doc links. If no clear section exists, append at the bottom under a new `## User manual` header.

    No other changes to either file.
  </action>
  <verify>
    <automated>grep -E "\\[`MANUAL\\.md`\\]\\(MANUAL\\.md\\)" README.md && grep -E "MANUAL\\.md" CURRENT-STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` `## 📚 More docs` section contains a bullet linking to `MANUAL.md` (regex `\[`MANUAL\.md`\]\(MANUAL\.md\)` matches).
    - `CURRENT-STATE.md` contains the string `MANUAL.md` somewhere (any link form is acceptable).
    - Neither file has other diff churn (the executor should make exactly the two additions described — no reformatting of surrounding lines).
  </acceptance_criteria>
  <done>MANUAL.md is reachable from the two canonical entry points (README on GitHub; CURRENT-STATE for the next agent or future-Flo).</done>
</task>

<task type="auto">
  <name>Task 3: Register UAT walkthrough as verify-phase gate (SUMMARY-only handoff)</name>
  <files></files>
  <read_first>
    .claude/skills/uat-agent/SKILL.md
    .research/UAT.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-VALIDATION.md
  </read_first>
  <action>
    This task ships nothing to a source file — it's a SUMMARY-level handoff that records the verify-phase contract for REQ-uat-walkthrough, REQ-edge-cases, REQ-gate-slider per D-15 + D-16. Confirm three things and record them in `02-05-SUMMARY.md`:

    1. `.claude/skills/uat-agent/SKILL.md` exists and the trigger phrase "run uat" walks `.research/UAT.md` interactively, one section at a time, updating checkboxes in place (verify by reading the skill file).
    2. `.research/UAT.md` contains §12 (gate slider — flagged ⚠️ suspect; bring MIDI monitor) and §19 (edge cases — hot-plug, refresh, style swap mid-hold) — verify by `grep -E "^## (12|19)" .research/UAT.md` (or whatever section-heading style the file uses; confirm by reading once).
    3. The Wave 0 additions called out in 02-VALIDATION.md and 02-RESEARCH.md (two new UAT checklist items for transport-sync coverage: §15.5 "Switch Int→Ext mid-play → panic + latch clear" and §15.6 "Ext mode does not echo clock pulses downstream") need to be added to `.research/UAT.md` before verify-phase. If they're not already present, ADD them as new `- [ ]` bullets under §15 of `.research/UAT.md` (this is the only optional content edit in this task — the rest is record-keeping).

    Do NOT run the UAT walkthrough here — `/gsd:verify-work` (verify-phase) invokes the `uat-agent` skill at the v1 close gate (D-15). Do NOT mark any UAT bullets as `[x]` — the agent + Flo do that interactively per the skill spec.
  </action>
  <verify>
    <automated>test -f .claude/skills/uat-agent/SKILL.md && test -f .research/UAT.md && grep -cE "Switch.*Int.*Ext" .research/UAT.md | awk '$1>=1{exit 0} {exit 1}' && grep -cE "Ext mode.*clock pulses|clock pulses.*downstream" .research/UAT.md | awk '$1>=1{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/uat-agent/SKILL.md` exists (precondition — already shipped).
    - `.research/UAT.md` contains a new bullet covering D-03 mode-switch hard stop (regex `Switch.*Int.*Ext` matches at least once).
    - `.research/UAT.md` contains a new bullet covering Pitfall 8 (Ext mode does NOT echo clock pulses) — regex matches at least once.
    - SUMMARY records: (a) UAT skill exists, (b) UAT sections 12 + 15 + 19 will exercise REQ-gate-slider + REQ-clock-send-transport-sync + REQ-edge-cases respectively, (c) verify-phase trigger is "run uat" per the skill's documented trigger phrase.
  </acceptance_criteria>
  <done>REQ-uat-walkthrough is formally handed off to verify-phase with `uat-agent`. REQ-edge-cases (§19) and REQ-gate-slider (§12 suspect) are explicitly registered as UAT checklist items, satisfying D-16 ("verified during UAT — no separate work item"). Two new transport-sync UAT bullets are in place so the walkthrough exercises plan 02-04's new behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub markdown renderer ← `MANUAL.md` | GitHub sanitizes markdown by default; we author markdown not raw HTML. No XSS surface. |
| User browser ← `README.md` / `CURRENT-STATE.md` link clicks | Plain markdown links to relative files — no cross-origin redirects, no JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | `MANUAL.md` content | accept | File is public-facing documentation; no secrets, no internal architecture leakage (D-13 explicitly says "how to use, not how it works internally"). |
| T-02-05-02 | Tampering (of UX expectation via stale docs) | `MANUAL.md` Section 4 | mitigate | This plan depends on 02-04 (Wave 2) shipping the transport sync behavior first. Authoring Section 4 AFTER the wiring lands means documented behavior matches shipped behavior — no drift. |
| T-02-05-03 | XSS via MANUAL.md content | GitHub-rendered markdown | accept | GitHub renders markdown with sanitization; we author markdown (not raw HTML). RESEARCH.md §Security Domain explicitly notes this. |
</threat_model>

<verification>
- `MANUAL.md` exists with 4 required H1 sections in order, ≥80 lines, no Note/Tip callout boxes.
- `README.md` 'More docs' links to MANUAL.md.
- `CURRENT-STATE.md` references MANUAL.md.
- `.research/UAT.md` has new bullets for mode-switch hard stop + Ext-mode-no-clock-echo.
- `just ci` passes (no source changes — clean diff).
</verification>

<success_criteria>
The manual ships per D-11..D-14. UAT walkthrough is registered as the verify-phase gate. Once `/gsd:verify-work` is invoked and `uat-agent` walks .research/UAT.md to completion (all sections `[x]` or documented `[-]` skip per Open Question Q4), v1 closes and the project can scope v2 (sequencer) via `/gsd:new-milestone`.
</success_criteria>

<output>
Create `.planning/phases/02-post-prototype-polish-uat-acceptance/02-05-SUMMARY.md` when done. Record: MANUAL.md final line count, the bulleted handoff (UAT trigger phrase, sections covering each UAT-only REQ, verify-phase next step), and a one-line confirmation that Phase 2 plans 02-01..02-05 collectively cover every Phase 2 REQ-ID per REQUIREMENTS.md.
</output>
