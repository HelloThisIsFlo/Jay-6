---
phase: 02-post-prototype-polish-uat-acceptance
plan: 09
type: execute
wave: 4
depends_on:
  - 02-08
files_modified:
  - src/state.svelte.ts
  - src/components/TopBar.svelte
  - src/components/PianoLayout.svelte
  - src/App.svelte
  - MANUAL.md
  - README.md
autonomous: true
gap_closure: true
requirements:
  - REQ-ipad-polish
  - REQ-style-selector
must_haves:
  truths:
    - "The style selector shows 'Beat' instead of 'Phrase Dur', and MANUAL.md/README describe it as 'Beat'."
    - "On iPad/iPhone, long-press / double-tap on any UI surface (top bar, pads, labels) does NOT select text."
    - "On iPhone in landscape, the keyboard is reachable — either visible or the viewport scrolls to it; the app is no longer unusable in landscape."
  artifacts:
    - path: "src/state.svelte.ts"
      provides: "STYLE_LABELS.phraseDur renamed to 'Beat'"
      contains: "Beat"
    - path: "src/components/PianoLayout.svelte"
      provides: "user-select:none on pads/labels"
      contains: "user-select"
  key_links:
    - from: "src/App.svelte"
      to: "landscape media query"
      via: "scroll-lock relaxed so off-screen content is reachable"
      pattern: "orientation|landscape|overflow"
---

<objective>
Close the three remaining UI/labelling gaps from UAT.

1. **"Phrase Dur" → "Beat" (test 10, minor).** The name is inaccurate — it's chord stabs at fixed durations, not a "phrase". User wants "Beat". Conceptual model: Arpeggio (Arp 1/2) vs Beat.
2. **iPad/iPhone text-selection not suppressed (test 19, minor).** Plan 02-03 added `user-select:none` to `.topbar`, but double-click still selects text on iPhone — the pads + labels (PianoLayout) and/or other surfaces aren't covered or it's overridden.
3. **iPhone landscape unusable (test 19, major).** In landscape on iPhone, the keys aren't visible AND the page can't scroll to reach them. The body scroll-lock from plan 02-03 (`@media (pointer: coarse) and (max-width: 1366px)` → `overflow:hidden; position:fixed`) traps the viewport so off-screen content is unreachable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-UAT.md

# CSS + label changes are presentation, not data/math → no unit test (DEC-tests-data-and-math-only).
# The phraseDur INTERNAL key/type stays stable (see Task 1 decision) — the fragile latch/engine
# state machine keys off 'phraseDur'; a full type rename is high-churn for a label change.

<interfaces>
From src/state.svelte.ts:
```typescript
export const STYLE_LABELS: Record<StyleKind, string> = {
  hold: 'Hold',
  arp1: 'Arp (Style 1, 8th)',
  arp2: 'Arp (Style 2, 16th)',
  phraseDur: 'Phrase Dur (Style 3)',   // ← rename label text only
  rhythm4: 'Rhythm Gate (Style 4)',
  rhythm5: 'Rhythm Gate (Style 5)',
};
// StyleKind union includes 'phraseDur' — keep the key
```

From src/App.svelte (scroll-lock origin is here, NOT TopBar):
```css
@media (pointer: coarse) and (max-width: 1366px) {
  :global(html), :global(body) {
    overflow: hidden; position: fixed; width: 100%; height: 100%;
    overscroll-behavior: none;
  }
}
```

From src/components/PianoLayout.svelte:
```css
.piano { user-select: none; touch-action: none; }   /* .piano has it; verify .pad inherits / labels covered */
.pad .key, .pad .name { /* text spans — confirm not selectable */ }
```

From src/components/TopBar.svelte:
```css
.topbar { user-select: none; -webkit-user-select: none; }  /* topbar covered (02-03) */
```

MANUAL.md:150 → `## 🎼 Phrase Dur — chord-as-rhythm`
README.md:11 → `... Phrase Duration ...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename user-facing 'Phrase Dur' → 'Beat' (label + docs)</name>
  <files>src/state.svelte.ts, MANUAL.md, README.md</files>
  <action>
    Change the USER-FACING label only:
    - state.svelte.ts: `STYLE_LABELS.phraseDur` → `'Beat'` (drop the "(Style 3)" parenthetical too, or keep style numbering consistent with siblings — match whatever reads cleanest; the gap wants 'Beat' to be the visible word). WHY-comment: 'Phrase Dur' was inaccurate — it's chord stabs at fixed beat durations (UAT test 10).
    - MANUAL.md:150: heading `## 🎼 Phrase Dur — chord-as-rhythm` → use 'Beat' as the name; keep the descriptive subtitle. Update any in-body prose that says "Phrase Duration" / "Phrase Dur" in that section.
    - README.md:11: `Phrase Duration` → `Beat` in the styles list.

    DECISION (resolve in-task, conservative default): the gap asks whether Rhythm Gate 4/5 also fold under a "Beat" umbrella. Default = NO — keep Rhythm Gate as its own labelled style; only rename Style 3. Rationale: folding Rhythm Gate under "Beat" is an information-architecture redesign (selector grouping), out of scope for a label-fix gap, and the visual-design todo already owns broader UI rework. Note this choice in the SUMMARY.

    DO NOT rename the internal `phraseDur` StyleKind key, the `PhraseDuration` type, or the `PhraseDurationEngine` class. They are internal identifiers keyed by the fragile latch/engine state machine (CONCERNS.md) and the App.svelte style map; renaming them is high-churn churn for zero user benefit and risks the state machine. The gap's "in-code identifiers consistent with new label" is satisfied by the label being authoritative — internal naming can track 'phraseDur' historically. Add a one-line WHY-comment near STYLE_LABELS noting the internal key stays 'phraseDur' for state-machine stability.
  </action>
  <verify>
    <automated>just check 2>&1 | tail -20; grep -rn "Phrase Dur\|Phrase Duration" src/state.svelte.ts MANUAL.md README.md</automated>
  </verify>
  <done>Style selector + MANUAL + README show 'Beat'; the grep returns no user-facing 'Phrase Dur'/'Phrase Duration' strings in those files; internal phraseDur key unchanged; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress text-selection on all touch surfaces</name>
  <files>src/components/PianoLayout.svelte, src/App.svelte</files>
  <action>
    The topbar already has `user-select:none` (02-03) but iPhone double-tap still selects text — extend coverage. On PianoLayout: ensure `.pad`, `.pad .key`, `.pad .name` all have `user-select: none; -webkit-user-select: none;` (the `.piano` container has it but the text spans + buttons may still be independently selectable on iOS, which is the most likely culprit for double-tap-selects-label). Also add `-webkit-touch-callout: none;` to suppress the iOS long-press callout/selection menu.

    On App.svelte root `main`: add a global-ish `user-select: none; -webkit-user-select: none; -webkit-touch-callout: none;` so the whole app surface is non-selectable by default (form inputs in TopBar remain editable — `<input>`/`<select>` keep their native behavior; if needed re-enable `user-select:text` on `input` only). WHY-comment: 02-03 only covered .topbar; iPhone double-tap still selected pad labels (UAT test 19) — suppress selection app-wide, re-enabling only for editable inputs.
  </action>
  <verify>
    <automated>just check 2>&1 | tail -20; grep -n "user-select\|touch-callout" src/components/PianoLayout.svelte src/App.svelte</automated>
  </verify>
  <done>Pads, labels, and the app root are non-selectable; -webkit-touch-callout suppressed; inputs remain editable. Manual UAT re-verify (test 19, iPhone double-tap) confirms no text selection.</done>
</task>

<task type="auto">
  <name>Task 3: Make iPhone landscape usable (reachable keyboard)</name>
  <files>src/App.svelte</files>
  <action>
    The 02-03 body scroll-lock (`@media (pointer: coarse) and (max-width: 1366px)` → `overflow:hidden; position:fixed; height:100%`) traps the viewport so in iPhone landscape (short height, content taller than viewport) the keys are off-screen AND unscrollable (UAT test 19).

    Fix: relax the scroll-lock so content stays reachable in constrained landscape while keeping the rubber-band/address-bar protection it was added for. Narrow the lock to portrait-ish / tall-enough viewports, OR allow vertical scroll when content overflows. Concretely:
    - Add an `and (orientation: landscape)` (or `and (max-height: 480px)`) override that restores `overflow-y: auto` (or removes `position: fixed`) on html/body so the user can scroll to the keys in landscape. Keep `overscroll-behavior: none` to retain rubber-band suppression.
    - Keep the original portrait lock intact (that's where the rubber-band fix matters and content fits).
    WHY-comment: original lock assumed content fits the viewport (true in portrait); iPhone landscape is shorter than the UI, so position:fixed trapped off-screen keys — relax to scrollable in short/landscape viewports while keeping overscroll containment (UAT test 19).

    Prefer the minimal CSS change (a scoped media-query override) over a layout rewrite — a responsive landscape redesign belongs to the visual-design todo, not this gap.
  </action>
  <verify>
    <automated>just check 2>&1 | tail -20</automated>
  </verify>
  <done>A landscape/short-viewport media override restores scrollability so off-screen keys are reachable; portrait lock + overscroll containment retained. Manual UAT re-verify (test 19, iPhone landscape) confirms keys reachable.</done>
</task>

</tasks>

<verification>
- `just check` + `just test` green (label + CSS → no new unit tests per convention).
- Manual UAT re-verify gate:
  - Test 10: style selector reads 'Beat'.
  - Test 19: iPhone double-tap selects no text; landscape reaches the keys.
</verification>

<success_criteria>
- 'Beat' label live in UI + docs; internal phraseDur key stable.
- No text-selection on any touch surface; inputs still editable.
- iPhone landscape is usable (keys reachable).
- Automated checks green.
</success_criteria>

<output>
Create `.planning/phases/02-post-prototype-polish-uat-acceptance/02-09-SUMMARY.md` when done.
</output>
