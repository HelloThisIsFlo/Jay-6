---
phase: 03-catalogue-mechanism-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/suggestions.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
  - PROG-05
  - PROG-06
  - PROG-07
  - BOOT-01

must_haves:
  truths:
    - "PROG-02 adjacency: repeated or consecutive pad keys remain distinct ordered steps; validation never deduplicates them."
    - "PROG-02 empty: an entry with an empty steps array is rejected because it cannot describe an ordered suggestion."
    - "PROG-02 ordering: every accepted step remains in its authored array position."
    - "PROG-03 empty: an empty top-level catalogue validates successfully, while missing fields and blank id or label values fail."
    - "PROG-03 encoding: valid Unicode labels and canonical sharp pad keys are preserved verbatim; whitespace-only or case-mismatched keys fail rather than being normalized."
    - "PROG-04 empty: blank canonical chord names remain blank in chordName and obtain their displayLabel only through labelFor()."
    - "PROG-04 encoding: canonical altered and slash chord labels are returned byte-for-byte from the bank exports."
    - "PROG-06 adjacency: neighbouring progression and movement records retain their individual kinds and are never coalesced."
    - "PROG-06 empty: a missing, blank, or unsupported kind is rejected."
    - "PROG-06 ordering: kind does not introduce grouping or sorting; source catalogue order remains authoritative."
    - "BOOT-01 adjacency: the two Bank 1 records are adjacent in their locked order and the Bank 14 record resolves independently."
    - "BOOT-01 empty: every bank other than 1 and 14 returns an exact empty array."
    - "BOOT-01 ordering: the production catalogue is exactly the three locked records, with both Bank 1 progressions before the Bank 14 movement."
  assumptions:
    - statement: "PROG-01 unclassified edge: direct edits to src/suggestions.data.json followed by the focused test command are the complete maintenance surface; no authoring UI, generator, or runtime import is implied."
      status: unresolved
      flagged: true
    - statement: "PROG-05 unclassified edge: zero, one, or many is defined per canonical integer bank index with no pagination, grouping, or alternate lookup identity."
      status: unresolved
      flagged: true
    - statement: "PROG-07 unclassified edge: pure Vitest coverage plus just ci is the complete Phase 3 proof because this phase has no UI, MIDI, hardware, network, or storage behavior."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: PROG-01
      category: transparency
      statement: "The catalogue mechanism must not silently become an authoring workflow, catalogue generator, or claim of comprehensive musical authority."
      status: unverified
      flagged: true
    - requirement_id: PROG-04
      category: product-integrity
      statement: "Suggestion data must not duplicate or invent factory bank names or chord names, including labels for unnamed stack-bank pads."
      status: unverified
      flagged: true
    - requirement_id: BOOT-01
      category: product-control
      statement: "Suggestion lookup must not audition, sequence, schedule, score, advance, or otherwise alter MIDI, playback, transport, clock, latch, or UI state."
      status: unverified
      flagged: true
  artifacts:
    - path: test/suggestions.test.ts
      provides: "Fail-first validator, production-catalogue, canonical-resolution, and lookup contract"
      contains: "describe('suggestion catalogue validation'"
  key_links:
    - from: test/suggestions.test.ts
      to: src/suggestions.ts
      via: "Direct imports of the public validator, catalogue, kinds, and lookup surface"
      pattern: "validateSuggestionCatalogue|getSuggestionsForBank|suggestionCatalogue|SUGGESTION_KINDS"
    - from: test/suggestions.test.ts
      to: src/banks.ts
      via: "Canonical bank anchors and fallback-aware expected labels"
      pattern: "Cadd9|FM/A|G/B|Oct Stack"
---

<objective>
Create the Wave 0 fail-first contract for the complete suggestion catalogue subsystem before production implementation.

Purpose: Make every locked authoring, validation, canonical-resolution, ordering, kind, and bootstrap behavior executable before `src/suggestions.ts` exists, covering D-01 through D-14 and all Phase 3 requirements.
Output: `test/suggestions.test.ts`, intentionally red only because the planned public module is absent.
</objective>

<execution_context>
@/Users/flo/.codex/gsd-core/workflows/execute-plan.md
@/Users/flo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-RESEARCH.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-PATTERNS.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-VALIDATION.md
@src/banks.ts
@test/banks.test.ts
@test/phrases.test.ts

<interfaces>
The test contract must import these planned exports from `../src/suggestions`:

- `SUGGESTION_KINDS`
- `validateSuggestionCatalogue(input: unknown): ValidationResult`
- `suggestionCatalogue`
- `getSuggestionsForBank(bankIndex: number)`

Existing canonical interfaces from `src/banks.ts`:

- `KEYS` is the twelve-value runtime pad-key allowlist.
- `Key` is derived from `KEYS`.
- `banks` is the ordered 100-bank canonical collection.
- `labelFor(bank, chord)` is the only fallback-label policy.
- `getBank(index)` wraps out-of-range indexes and therefore is not a validation primitive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify the pure staged validator contract</name>
  <files>test/suggestions.test.ts</files>
  <read_first>
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md` for D-01 through D-09.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-RESEARCH.md` sections “Staged unknown Validation”, “Structured Diagnostics Before Throwing”, and “Required Test Cases”.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-VALIDATION.md` for the Wave 0 test filename and sampling command.
    - `test/banks.test.ts` and `test/phrases.test.ts` for Vitest imports, focused describe blocks, exact equality, exhaustive loops, and local helper style.
    - `src/banks.ts` for `KEYS`, bank bounds, and the wrapping behavior that tests must guard against.
  </read_first>
  <action>
Create `test/suggestions.test.ts` and a local `validEntry(overrides)` raw-fixture builder. Import the planned `SUGGESTION_KINDS` and `validateSuggestionCatalogue` exports. Per D-01 through D-05, assert that a flat array is the only accepted top-level shape; each accepted record has exactly `id`, `bankIndex`, `label`, `kind`, and ordered `steps`; bankIndex is an integer in 1..100; step values come from canonical `KEYS`; an empty steps array fails; both supported kinds pass; missing, blank, and unsupported kinds fail; source order and repeated steps are preserved; and unexpected fields are rejected rather than discarded. Include Unicode label and sharp-key fixtures to prove string values are preserved without trimming or case folding.

Per D-06 through D-09, assert `validateSuggestionCatalogue(input: unknown)` returns a discriminated `{ ok: true, value }` or `{ ok: false, issues }` result, never mutates its input, and reports every independent issue in deterministic order. Pin stage order to top-level shape, entry source order, fields `id`, `bankIndex`, `label`, `kind`, `steps`, unexpected fields, step indexes, then cross-entry checks. Pin each `CatalogueIssue` to machine-stable `code`, JSONPath-like `path`, offending `value`, and `expected`, with `entryId` and `bankIndex` whenever those fields are independently valid. Cover primitive/null/array entries, missing and unexpected fields, whitespace-only or padded IDs and labels, bank values 0/101/fraction/string, non-array and empty steps, mixed valid/invalid step values, duplicate IDs, and same-bank duplicate ordered sequences. Duplicate diagnostics must identify the later path plus `relatedPath` for the first valid occurrence; run duplicate ID before duplicate sequence for each later valid record. Assert the same sequence in different banks and a reordered sequence in the same bank are accepted, and malformed records never enter duplicate checks. D-09 governs execution: mechanically repair obvious test-data formatting errors and rerun without pausing; escalate only if an existing curated meaning would be destructively reinterpreted.
  </action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('test/suggestions.test.ts','utf8'); for (const x of ['validateSuggestionCatalogue','SUGGESTION_KINDS','duplicate-id','duplicate-sequence','relatedPath','input unchanged','empty steps']) if (!s.includes(x)) throw new Error('missing validator contract: '+x)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/suggestions.test.ts` contains direct synthetic-`unknown` coverage for every validation rule in PROG-02, PROG-03, and PROG-06.
    - Exact expected issue arrays prove stage order, source order, field order, step order, duplicate order, locations, values, rules, and related locations.
    - The suite proves both successful and unsuccessful validation leave the original fixture deeply equal to a pre-call clone.
    - The suite proves an empty catalogue is valid but an entry with no steps is invalid.
    - No production file is created or modified in this task.
  </acceptance_criteria>
  <done>The validator's complete fail-closed behavior and diagnostic contract are fixed in an executable test file before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Specify bundled data, canonical resolution, and lookup</name>
  <files>test/suggestions.test.ts</files>
  <read_first>
    - `test/suggestions.test.ts` as modified by Task 1.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md` for D-10 through D-14 and exact sequence order.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-RESEARCH.md` sections “Prescribed Bootstrap Records”, “Canonical Projection”, and “Required Test Cases”.
    - `src/banks.ts` for canonical `banks` and `labelFor()` behavior.
    - `test/banks.test.ts` for Bank 1 and Bank 14 anchored values.
  </read_first>
  <action>
Extend `test/suggestions.test.ts` with production-catalogue, canonical-resolution, and lookup describe blocks importing `suggestionCatalogue` and `getSuggestionsForBank`. Per D-10 through D-14, assert exactly three records and the exact discretion-approved fixtures: `{ id: 'pop-homeward', bankIndex: 1, label: 'Homeward', kind: 'progression', steps: ['C','A','B','C'] }`, then `{ id: 'pop-step-up', bankIndex: 1, label: 'Step up', kind: 'progression', steps: ['C','D','F','C'] }`, then `{ id: 'oct-stack-rise-fall', bankIndex: 14, label: 'Rise and fall', kind: 'movement', steps: ['C','D','E','D'] }`. Assert the source data has no fields beyond D-02 and includes both kinds.

Assert Bank 1 resolves two suggestions in that exact order. The first canonical step names must be `Cadd9`, `FM/A`, `G/B`, `Cadd9`; the second must be `Cadd9`, `Dm7`, `FM9`, `Cadd9`. Assert Bank 14 resolves one movement whose raw `chordName` values remain empty while `displayLabel` values are `C Oct Stack`, `D Oct Stack`, `E Oct Stack`, `D Oct Stack`. Assert resolved suggestions expose `id`, `bankIndex`, `bankName`, `label`, `kind`, and ordered resolved steps containing only `key`, `chordName`, and `displayLabel`; they expose no notes or behavior callbacks. Exhaustively loop bank indexes 1..100 and assert every bank except 1 and 14 returns `[]`. Assert lookup rejects 0, 101, fractional, `NaN`, and infinities with `RangeError`, proving bounds are checked before any wrapping bank helper can run. Finish by invoking the focused suite and confirm its only failure is the absent `../src/suggestions` module; a syntax error or unrelated baseline failure does not satisfy the fail-first gate.
  </action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('test/suggestions.test.ts','utf8'); for (const x of ['pop-homeward','pop-step-up','oct-stack-rise-fall','getSuggestionsForBank','C Oct Stack','RangeError']) if (!s.includes(x)) throw new Error('missing resolver contract: '+x); const {spawnSync}=require('node:child_process'); const r=spawnSync('npm',['test','--','test/suggestions.test.ts'],{encoding:'utf8'}); const out=(r.stdout||'')+(r.stderr||''); process.stdout.write(out); if(r.status===0 || !/src\/suggestions|\.\.\/src\/suggestions/.test(out)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Exact deep equality fixes the three production records, their array order, their allowed fields, and their bank distribution.
    - Canonical Bank 1 slash/altered labels and Bank 14 empty-name fallbacks are asserted independently.
    - The loop over all 100 bank indexes proves 0/1/2 lookup cardinalities and exact empty arrays for the other 98 banks.
    - Invalid public lookup indexes are asserted to throw rather than wrap.
    - The focused suite is red specifically because `src/suggestions.ts` does not exist yet.
  </acceptance_criteria>
  <done>The entire Phase 3 behavior contract exists before the production catalogue and resolver are implemented.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/suggestions.data.json`
  - one flat array containing exactly three records with fields `id`, `bankIndex`, `label`, `kind`, and `steps`
- `src/suggestions.ts`
  - `SUGGESTION_KINDS`
  - `SuggestionKind`
  - `Suggestion`
  - `CatalogueIssueCode`
  - `CatalogueIssue`
  - `ValidationResult`
  - `ResolvedSuggestionStep`
  - `ResolvedSuggestion`
  - `validateSuggestionCatalogue(input)`
  - `suggestionCatalogue`
  - `getSuggestionsForBank(bankIndex)`
- `test/suggestions.test.ts`
  - Wave 0 validator, production data, canonical resolution, deterministic lookup, and exhaustive empty-bank contract

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent-authored JSON to typed domain data | Locally edited static values must be treated as untrusted `unknown` until exact validation succeeds. |
| Public bank index to canonical bank lookup | Caller-provided numeric indexes must not reach the wrapping `getBank()` behavior without bounds validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `validateSuggestionCatalogue` contract | medium | mitigate | Wave 0 tests require exact shape, positive allowlists, integer bounds, staged duplicate checks, deterministic diagnostics, and proof that malformed records never enter resolved data. |
| T-03-02 | Tampering | `getSuggestionsForBank` contract | medium | mitigate | Boundary tests require `RangeError` for every non-integer or out-of-range index before canonical array access, preventing modulo remapping. |
| T-03-03 | Information Disclosure | Catalogue diagnostics | low | accept | Diagnostics intentionally include offending local catalogue values for repair; the catalogue contains no secrets, credentials, network data, or personal data. |
| T-03-SC | Tampering | Package supply chain | low | accept | The plan installs no npm package and leaves `package.json` and `package-lock.json` unchanged; existing locked tooling only is used. |
</threat_model>

<verification>
- `test/suggestions.test.ts` contains both validator and resolver contracts.
- The focused Vitest run fails only at the absent production module boundary.
- No file under `src/` is changed by this plan.
- All 16 spec-less edge-probe items are represented by 13 testable truths and 3 flagged assumptions.
- All three surviving project-value prohibitions are recorded descriptor-less, flagged, and unverified.
</verification>

<success_criteria>
- Every Phase 3 requirement has executable expectations before implementation begins.
- Every D-01 through D-14 decision is pinned by a named test case or exact fixture assertion.
- The contract distinguishes raw canonical names from fallback-aware display labels.
- The fail-first state is attributable only to the missing planned source module.
</success_criteria>

<output>
Create `.planning/phases/03-catalogue-mechanism-bootstrap/03-01-SUMMARY.md` when done.
</output>
