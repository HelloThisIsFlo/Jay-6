---
phase: 03-catalogue-mechanism-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - test/suggestions.test.ts
  - src/suggestions.ts
autonomous: true
gap_closure: true
requirements:
  - PROG-02
  - PROG-03
  - PROG-07

estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Sparse top-level catalogue slots and sparse steps slots are traversed by index, reported at their exact JSONPath-like positions, and never projected as trusted undefined values (PROG-02, PROG-03; D-06, D-07, D-08)."
    - "A record passes the exact five-field contract only when every own string or symbol key is one of the five allowed string fields; non-enumerable and symbol extras fail with deterministic actionable issues (PROG-03; D-02, D-07, D-08)."
    - "Automated regressions prove validator and resolver outputs use fresh arrays, records, nested step arrays, and resolved step objects instead of caller-owned or prior-result references (PROG-07; D-06)."
  artifacts:
    - path: test/suggestions.test.ts
      provides: "Fail-first sparse-array, exact-own-key, and fresh-reference regression contract"
      contains: "sparse top-level"
    - path: src/suggestions.ts
      provides: "Fail-closed dense unknown traversal and exact own-key validation"
      exports:
        - validateSuggestionCatalogue
        - getSuggestionsForBank
  key_links:
    - from: test/suggestions.test.ts
      to: src/suggestions.ts
      via: "Public validator calls with adversarial sparse arrays and hidden own keys"
      pattern: "validateSuggestionCatalogue"
    - from: src/suggestions.ts
      to: ValidationResult
      via: "Dense indexed scans construct fresh typed projections only after every physical array slot and own record key passes"
      pattern: "Reflect\\.ownKeys|sourceIndex|stepIndex"
    - from: test/suggestions.test.ts
      to: getSuggestionsForBank
      via: "Two independent lookups prove fresh nested resolver projections"
      pattern: "getSuggestionsForBank"
---

<objective>
Close the verified fail-open cases at the catalogue's public `unknown` boundary and protect the already-working fresh projection behavior with identity regressions.

Purpose: Restore the Phase 3 claim that malformed catalogue shapes fail closed before any trusted `Suggestion` reaches application consumers.
Output: Focused adversarial regressions in `test/suggestions.test.ts` and a dense, all-own-key validation repair in `src/suggestions.ts`.
</objective>

<execution_context>
@/Users/flo/.codex/gsd-core/workflows/execute-plan.md
@/Users/flo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-VERIFICATION.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-02-SUMMARY.md
@src/suggestions.ts
@test/suggestions.test.ts

<interfaces>
Existing public contracts remain unchanged:

- `validateSuggestionCatalogue(input: unknown): ValidationResult`
- `ValidationResult` returns either a fresh `readonly Suggestion[]` projection or ordered `CatalogueIssue[]` diagnostics.
- `CatalogueIssue` carries stable `code`, `path`, `value`, and `expected` fields plus available entry/bank context.
- `getSuggestionsForBank(bankIndex: number): readonly ResolvedSuggestion[]` returns fresh inert canonical views.
- `Suggestion` retains exactly `id`, `bankIndex`, `label`, `kind`, and `steps` per D-02.

No new public production interface is needed for this gap closure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-first adversarial boundary and identity regressions</name>
  <files>test/suggestions.test.ts</files>
  <read_first>
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-VERIFICATION.md` for the reproduced sparse-array, hidden-own-key, and reference-identity gaps only.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md` for D-02 and D-06 through D-08; retain the existing staged diagnostic and exact-record contracts.
    - `test/suggestions.test.ts` for the local `validEntry` builder, exact issue-object style, purity assertions, and resolver describe blocks.
    - `src/suggestions.ts` for the existing twelve issue codes and public validator/resolver signatures; do not add a new public error code or API.
  </read_first>
  <behavior>
    - A one-slot sparse top-level array fails with `entry-object` at `$[0]` and offending value `undefined`.
    - A nonempty sparse `steps` array fails with `invalid-step` at every hole's exact step index and never returns `ok: true`.
    - An otherwise-valid record with a non-enumerable extra string field fails with `unexpected-field` at `$[0].hidden`.
    - An otherwise-valid record with `Symbol('hidden')` fails with `unexpected-field` at `$[0][Symbol(hidden)]`.
    - A successful validator result owns a new catalogue array, entry object, and nested steps array while remaining deeply equal to the input.
    - Separate resolver calls own distinct result arrays, suggestion records, nested steps arrays, and resolved step objects while remaining deeply equal.
  </behavior>
  <action>
Extend the existing validation describe block with four focused adversarial cases named exactly `rejects a sparse top-level catalogue slot`, `rejects sparse steps at each physical slot`, `rejects a non-enumerable unexpected own field`, and `rejects a symbol unexpected own field`, preserving D-02 and D-06 through D-08. Construct a sparse top-level array with an allocated length and no value at index 0; assert one exact `entry-object` issue at `$[0]`. Construct a valid entry whose nonempty steps array has an omitted physical slot between canonical keys; assert an exact `invalid-step` issue at that omitted index with `value: undefined`. Construct separate otherwise-valid records with `Object.defineProperty(record, 'hidden', { value: 'extra', enumerable: false })` and a locally held `Symbol('hidden')` key whose value is `extra`; assert each is rejected as `unexpected-field`. Pin the string-key path to `$[0].hidden` and the symbol-key path to `$[0][Symbol(hidden)]`, together with offending value, existing expected-rule text, and independently valid entry/bank context. Keep each test on the public `validateSuggestionCatalogue` surface so the red state reproduces the real trust-boundary failures rather than an internal helper.

Strengthen the existing successful-validation assertion: first retain deep equality, then narrow the discriminated result and use identity assertions to prove the returned catalogue array, first record, and first record's steps are not the caller's references. Add a resolver identity case that calls `getSuggestionsForBank(1)` twice and proves the two results are deeply equal but do not share their result arrays, first suggestion objects, nested steps arrays, or first resolved step objects. Do not assert runtime freezing because the established contract is fresh projection separation, not deep-freeze semantics.

Run the focused suite before modifying production code. It must fail on the newly added sparse/hidden-key assertions while existing catalogue, lookup, ordering, and canonical-name cases remain green. A syntax, import, or unrelated baseline failure does not satisfy this fail-first gate. Comments, if any, explain only why an adversarial fixture is constructed unusually.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process'); const expected=['rejects a sparse top-level catalogue slot','rejects sparse steps at each physical slot','rejects a non-enumerable unexpected own field','rejects a symbol unexpected own field']; const r=spawnSync('./node_modules/.bin/vitest',['run','test/suggestions.test.ts','--reporter=json'],{encoding:'utf8'}); process.stderr.write(r.stderr||''); let report; try{report=JSON.parse(r.stdout)}catch(error){process.stdout.write(r.stdout||''); throw new Error('Vitest did not produce structured results; collection, transform, or import failures cannot satisfy the RED gate')} const results=report.testResults||[]; const assertions=results.flatMap(result=>result.assertionResults||[]); const failed=assertions.filter(test=>test.status==='failed'); if(r.status===0||report.success!==false) throw new Error('expected the four adversarial regressions to fail before the repair'); if(results.length!==1||!results[0].name.endsWith('test/suggestions.test.ts')) throw new Error('focused file was not the sole collected suite'); if(report.numFailedTests!==4||failed.length!==4) throw new Error('RED gate requires exactly four assertion failures'); for(const title of expected){const matches=assertions.filter(test=>test.title===title); if(matches.length!==1||matches[0].status!=='failed'||matches[0].failureMessages.length===0) throw new Error('missing intended assertion failure: '+title)} const existing=assertions.filter(test=>!expected.includes(test.title)); if(existing.length&lt;42||existing.some(test=>test.status!=='passed')) throw new Error('a pre-existing or identity regression is not green'); if(report.numPassedTests!==existing.length||report.numPendingTests!==0||report.numTodoTests!==0||report.numTotalTests!==assertions.length) throw new Error('structured counts do not match four intended failures plus green regressions'); console.log('RED verified: four intended adversarial assertions fail; '+existing.length+' other assertions pass')"</automated>
  </verify>
  <acceptance_criteria>
    - Sparse top-level and sparse steps fixtures call the public validator and pin exact hole locations and `undefined` offending values.
    - Symbol and non-enumerable extra-own-field fixtures fail the same exact-field contract as normal enumerable string extras.
    - Validator identity assertions distinguish deep value equality from array, record, and nested-steps reference separation.
    - Resolver identity assertions distinguish deep value equality from array, record, steps-array, and resolved-step reference separation across calls.
    - The focused suite is red because at least one newly added adversarial expectation fails, with no production file changed in this task.
  </acceptance_criteria>
  <done>The authoritative verification gaps are reproduced through the same public APIs future catalogue edits and Phase 4 consumers rely on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the unknown boundary dense and exact over every own key</name>
  <files>src/suggestions.ts</files>
  <read_first>
    - `test/suggestions.test.ts` as modified by Task 1 for the exact failing diagnostics and reference-identity contract.
    - `src/suggestions.ts` for `validateSuggestionCatalogue`, `ENTRY_FIELDS`, `ENTRY_FIELD_SET`, the valid-entry projection, and `getSuggestionsForBank`.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md` for D-02 and D-06 through D-08; do not alter the catalogue shape, stage order, or diagnostic vocabulary.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-02-SUMMARY.md` for the established unknown-to-validated and fresh-projection patterns that this repair must preserve.
  </read_first>
  <behavior>
    - Every numeric position from 0 through `input.length - 1` is visited even when no property exists at that index.
    - Every numeric position from 0 through `steps.length - 1` is visited and participates in key validation even when no property exists at that index.
    - Exact-field enforcement sees enumerable strings, non-enumerable strings, and symbols while preserving deterministic own-key order.
    - Only completely valid dense records enter duplicate checks and fresh typed projection.
    - Existing issue codes, staged order, catalogue order, canonical resolution, and public signatures do not change.
  </behavior>
  <action>
Repair `validateSuggestionCatalogue` without changing its public types or the D-02 record contract. Replace the top-level callback traversal with an indexed loop from zero to `input.length - 1`, reading `input[sourceIndex]` so an omitted slot is validated as `undefined` at its actual catalogue position. Replace step callback traversal with an indexed loop over the declared length, reading `steps[stepIndex]` so every omitted slot emits `invalid-step` at the correct path. Carry an explicit dense-step validity signal into the valid-entry gate; do not use a hole-skipping array predicate to certify `readonly Key[]`. Preserve the established stage ordering: record/field issues first, own-key extras next, indexed step issues next, then valid-record-only duplicate checks.

Replace enumerable-string-only exact-field inspection with `Reflect.ownKeys(candidate)`. Treat every symbol key as unexpected and every string key outside `ENTRY_FIELD_SET` as unexpected, including non-enumerable properties. Use the existing `unexpected-field` code and `ENTRY_FIELD_RULE`; derive a deterministic path from the key, retaining dot notation for string keys and a bracketed `String(symbol)` representation for symbols, and read the offending value with reflection so symbol values are included. Keep valid own fields accepted regardless of enumeration mechanics, copy only the five known fields into the fresh projection, and never spread or return the raw record.

Run the focused suite until every new regression and all existing 41-era behaviors pass, then run `just check` and `just ci`. Do not modify `src/suggestions.data.json`, `src/banks.data.json`, the twelve issue-code union, public interfaces, bootstrap content, resolver semantics, or catalogue ordering. Preserve WHY-only comments.
  </action>
  <verify>
    <automated>npm test -- test/suggestions.test.ts &amp;&amp; just check &amp;&amp; just ci</automated>
  </verify>
  <acceptance_criteria>
    - Both sparse-array regressions now return the exact ordered failure objects fixed by Task 1 and never project `undefined` as a trusted `Key` or catalogue entry.
    - Both hidden-own-key regressions now return `unexpected-field` with deterministic paths, values, rules, and entry context.
    - All fresh-reference identity assertions pass for validator and resolver projections.
    - Existing malformed-input diagnostics, duplicate ordering, exact bootstrap, canonical labels, source-order lookup, and 98 empty-bank assertions remain green.
    - `just check` and `just ci` pass, and neither catalogue data file nor any public export changes.
  </acceptance_criteria>
  <done>The public unknown boundary fails closed for sparse arrays and every extra own key while preserving fresh, deterministic validated and resolved projections.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new production file or public production symbol is needed.
- Modified `src/suggestions.ts`
  - Dense indexed validation for top-level and steps arrays.
  - Full own-key exact-field enforcement using the existing diagnostic contract.
- Modified `test/suggestions.test.ts`
  - Sparse top-level array regression.
  - Sparse steps regression.
  - Symbol and non-enumerable extra-own-field regressions.
  - Validator and resolver fresh-reference identity regressions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent-authored or synthetic `unknown` data to typed suggestions | JavaScript arrays and records can carry holes, symbols, and non-enumerable properties that JSON normally does not express; the public validator must still fail closed for its declared `unknown` input. |
| Validated catalogue to application consumer | Consumers must receive fresh known-field projections rather than caller-owned or canonical mutable references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-G1 | Tampering | `validateSuggestionCatalogue` array traversal | high | mitigate | Indexed top-level and step scans validate every declared position, including holes, before a record can enter the trusted projection; focused regressions exercise both levels. |
| T-03-G2 | Tampering | Exact record-shape enforcement | high | mitigate | `Reflect.ownKeys` inspects string, symbol, enumerable, and non-enumerable own fields; only the five D-02 string keys survive projection. |
| T-03-G3 | Tampering | Validator and resolver output ownership | medium | mitigate | Public tests assert fresh arrays and nested objects at both validation and resolution boundaries so caller or prior-result mutation cannot alias new projections. |
| T-03-G4 | Denial of Service | Dense traversal of local catalogue arrays | low | accept | Work remains linear in declared array lengths, the production source is a three-record bundled JSON file, and adversarial synthetic inputs are test/developer controlled. |
| T-03-SC | Tampering | Package supply chain | low | accept | No package is installed or changed; the repair uses standard indexed loops, reflection, existing TypeScript, and existing Vitest tooling. |
</threat_model>

<verification>
- Task 1 proves the tests fail for the reproduced public-boundary behaviors before production repair.
- `npm test -- test/suggestions.test.ts` proves sparse arrays, all own-key categories, fresh identities, and the entire existing suggestion contract together.
- `just check` proves strict TypeScript/Svelte correctness without widening public types.
- `just ci` proves the full project suite and production build remain green.
- `git diff -- src/suggestions.data.json src/banks.data.json` remains empty; catalogue and canonical data are outside this gap closure.
</verification>

<success_criteria>
- No sparse slot can disappear from validation or enter a trusted projection as `undefined`.
- No symbol or non-enumerable extra own field can bypass the exact five-field record contract.
- Fresh validator and resolver projection guarantees are executable, behavior-level regressions rather than deep-equality assumptions.
- The repair preserves every already-verified Phase 3 truth and introduces no UI, catalogue content, dependency, schema, or public API change.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature or requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | — | Trustworthy bank-aware suggestion maintenance without application-code edits for content changes | 03-03 | COVERED | Closes the only reproduced trust-boundary failures; the maintenance surface remains the existing JSON file. |
| REQ | PROG-01 | One plain agent-editable catalogue | 03-01, 03-02 | COVERED | Already verified; no gap-closure task changes the maintenance surface. |
| REQ | PROG-02 | Factory bank plus ordered valid pad keys | 03-03 | COVERED | Dense step traversal prevents an omitted slot from becoming a trusted key. |
| REQ | PROG-03 | Reject malformed and non-exact entries | 03-03 | COVERED | Sparse entries/steps and hidden own fields fail closed. |
| REQ | PROG-04 | Resolve names from canonical bank data | 03-01, 03-02 | COVERED | Already verified; resolver identity tests preserve the existing canonical projection without changing naming. |
| REQ | PROG-05 | Deterministic zero/one/many lookup | 03-01, 03-02 | COVERED | Already verified; full regression suite remains required. |
| REQ | PROG-06 | Preserve progression and movement kinds | 03-01, 03-02 | COVERED | Already verified; no kind behavior changes. |
| REQ | PROG-07 | Automated integrity and resolution proof | 03-03 | COVERED | Adds the missing adversarial and reference-identity regressions. |
| REQ | BOOT-01 | Exact tiny bootstrap and honest empty banks | 03-01, 03-02 | COVERED | Already verified; both data files remain untouched and full CI rechecks the behavior. |
| RESEARCH | — | Strict `unknown` to validated readonly boundary | 03-03 | COVERED | Authoritative verification refines the earlier implementation pattern with dense traversal and all-own-key inspection. |
| RESEARCH | — | Pure fresh known-field projections | 03-03 | COVERED | Identity assertions protect validator and resolver arrays and nested objects. |
| RESEARCH | — | Staged diagnostics and valid-record-only duplicate checks | 03-02, 03-03 | COVERED | The repair preserves stage order and excludes hole-bearing records before duplicates. |
| RESEARCH | — | Canonical resolution and source-array ordering | 03-01, 03-02 | COVERED | Already verified and retained under the full focused suite. |
| RESEARCH | — | No package installation; Vitest plus `just ci` | 03-03 | COVERED | Existing locked tooling only. |
| CONTEXT | D-01 | One flat JSON catalogue | 03-01, 03-02 | COVERED | Existing artifact unchanged. |
| CONTEXT | D-02 | Exactly five record fields; ordered canonical-key steps | 03-03 | COVERED | All own keys are inspected and sparse steps cannot pass. |
| CONTEXT | D-03 | Bank indexes 1..100; canonical names only | 03-01, 03-02 | COVERED | Already verified and outside the gap implementation. |
| CONTEXT | D-04 | Array order is suggestion order | 03-01, 03-02 | COVERED | Dense iteration retains source indexes and the resolver remains unsorted. |
| CONTEXT | D-05 | No speculative metadata | 03-01, 03-02, 03-03 | COVERED | Exact all-own-key enforcement also rejects hidden metadata. |
| CONTEXT | D-06 | Pure non-mutating validation | 03-03 | COVERED | Fresh identity tests pin non-aliasing projections. |
| CONTEXT | D-07 | Staged complete deterministic validation | 03-03 | COVERED | Holes and hidden keys join the existing deterministic stage order. |
| CONTEXT | D-08 | Actionable contextual diagnostics | 03-03 | COVERED | Regressions pin exact hole and hidden-key paths, values, rules, and context. |
| CONTEXT | D-09 | Mechanical repair without unnecessary escalation | 03-01, 03-02 | COVERED | Existing execution rule remains intact; no curation conflict is introduced. |
| CONTEXT | D-10 | Exactly three records across two banks | 03-01, 03-02 | COVERED | Existing data remains untouched. |
| CONTEXT | D-11 | Exact ordered Bank 1 progressions | 03-01, 03-02 | COVERED | Existing focused tests remain part of verification. |
| CONTEXT | D-12 | Exact Bank 14 movement and fallback path | 03-01, 03-02 | COVERED | Existing focused tests remain part of verification. |
| CONTEXT | D-13 | Other 98 banks resolve empty | 03-01, 03-02 | COVERED | Existing exhaustive test remains part of verification. |
| CONTEXT | D-14 | Fixtures make no comprehensive-authority claim | 03-01, 03-02 | COVERED | No catalogue content or authoring workflow is added. |

Deferred catalogue expansion, Phase 4 suggestion rendering, Phase 5 variation work, selector exploration, Phase 6 external BPM, and transport work remain excluded. The spec-less deterministic edge probes already resolved by Plans 03-01 and 03-02 are preserved through the full focused suite; only the authoritative verification gaps receive new predicates here.
</source_audit>

<output>
Create `.planning/phases/03-catalogue-mechanism-bootstrap/03-03-SUMMARY.md` when done.
</output>
