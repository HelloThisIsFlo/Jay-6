---
phase: 03-catalogue-mechanism-bootstrap
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - test/suggestions.test.ts
  - src/suggestions.ts
autonomous: true
gap_closure: true
requirements:
  - PROG-02
  - PROG-03
  - PROG-07

estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Sparse catalogue and steps slots accept only own data-property values; inherited, missing, and accessor slots fail at their exact positions without invoking getters (PROG-02, PROG-03; D-06, D-07, D-08)."
    - "Every allowed record field is read only from its own data-property descriptor; inherited fields remain missing and accessor fields return existing field-specific validation issues without throwing or side effects (PROG-03; D-02, D-06, D-07, D-08)."
    - "An authored id or label contains at least one code point outside Unicode whitespace, format, and control categories while preserving valid authored Unicode and the existing no-surrounding-whitespace rule (PROG-03; D-06, D-08)."
    - "Unexpected safe string keys retain dot paths, unsafe strings use escaped bracket paths, and symbols carry stable own-key ordinals so every issue path is single-line and unambiguous (PROG-03; D-07, D-08)."
    - "Public regressions fail before the repair and the complete focused/full suite passes afterward without changing catalogue data, canonical data, resolver semantics, public types, issue codes, or diagnostic stage order (PROG-07; D-01 through D-14)."
  assumptions:
    - statement: "PROG-01 unclassified edge: direct edits to the single JSON catalogue remain the complete maintenance surface; the requirement does not define another authoring channel."
      status: unresolved
      flagged: true
    - statement: "PROG-02 adjacency edge: neighbouring and repeated step positions remain separate authored positions when descriptor-safe traversal is applied."
      status: unresolved
      flagged: true
    - statement: "PROG-02 empty edge: a missing, accessor-backed, or empty step position is invalid rather than inherited or synthesized."
      status: unresolved
      flagged: true
    - statement: "PROG-02 ordering edge: own data-property steps retain their declared numeric index order."
      status: unresolved
      flagged: true
    - statement: "PROG-03 empty edge: empty and visually blank authored text, missing data descriptors, and empty steps fail closed."
      status: unresolved
      flagged: true
    - statement: "PROG-03 encoding edge: valid Unicode remains verbatim while format/control-only text and unsafe diagnostic-key encodings are rejected or escaped deterministically."
      status: unresolved
      flagged: true
    - statement: "PROG-04 empty edge: blank canonical chord names remain canonical raw values and continue to receive display fallback only through labelFor()."
      status: unresolved
      flagged: true
    - statement: "PROG-04 encoding edge: canonical altered, slash, and Unicode chord labels remain unchanged by catalogue validation."
      status: unresolved
      flagged: true
    - statement: "PROG-05 unclassified edge: zero, one, or many remains defined per canonical integer bank index with no alternate grouping or lookup identity."
      status: unresolved
      flagged: true
    - statement: "PROG-06 adjacency edge: adjacent progression and movement records retain separate kinds."
      status: unresolved
      flagged: true
    - statement: "PROG-06 empty edge: missing, blank, accessor-backed, or unsupported kind fields fail through the existing invalid-kind contract."
      status: unresolved
      flagged: true
    - statement: "PROG-06 ordering edge: kind never groups or sorts catalogue records; source order remains authoritative."
      status: unresolved
      flagged: true
    - statement: "PROG-07 unclassified edge: public Vitest regressions plus just ci are the complete Phase 3 proof because the phase has no UI, MIDI, hardware, network, or storage behavior."
      status: unresolved
      flagged: true
    - statement: "BOOT-01 adjacency edge: the two Bank 1 records remain adjacent and Bank 14 remains the independent third record."
      status: unresolved
      flagged: true
    - statement: "BOOT-01 empty edge: every bank other than 1 and 14 continues to resolve to an exact empty array."
      status: unresolved
      flagged: true
    - statement: "BOOT-01 ordering edge: the exact three bootstrap records retain their locked source order."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: PROG-01
      category: transparency
      statement: "The catalogue mechanism must not silently become an authoring workflow, catalogue generator, or claim of comprehensive musical authority."
      status: unverified
      flagged: true
    - requirement_id: PROG-04
      category: product-integrity
      statement: "Suggestion data must not duplicate or invent factory bank names or chord names, including labels for unnamed stack-bank pads."
      status: unverified
      flagged: true
    - requirement_id: BOOT-01
      category: product-control
      statement: "Suggestion lookup must not audition, sequence, schedule, score, advance, or otherwise alter MIDI, playback, transport, clock, latch, or UI state."
      status: unverified
      flagged: true
  artifacts:
    - path: test/suggestions.test.ts
      provides: "Fail-first public regressions for descriptor safety, visible text, and stable diagnostic paths"
      contains: "does not invoke getters"
    - path: src/suggestions.ts
      provides: "Descriptor-only unknown validation and unambiguous single-line issue paths"
      exports:
        - validateSuggestionCatalogue
  key_links:
    - from: test/suggestions.test.ts
      to: src/suggestions.ts
      via: "Adversarial fixtures exercise only the exported validateSuggestionCatalogue boundary"
      pattern: "validateSuggestionCatalogue"
    - from: src/suggestions.ts
      to: ValidationResult
      via: "Own descriptors are inspected without invoking accessors before known-field projection or issue construction"
      pattern: "Object\.getOwnPropertyDescriptor|Reflect\.ownKeys"
    - from: CatalogueIssue.path
      to: formatCatalogueIssue
      via: "Escaped one-line paths remain stable when formatted into import-time diagnostics"
      pattern: "path=|formatCatalogueIssue"
---

<objective>
Close the four remaining public-validator gaps that block PROG-02, PROG-03, and PROG-07 while preserving every already-verified Phase 3 behavior and D-01 through D-14.

Purpose: Make the declared `unknown` boundary genuinely fail closed for JavaScript prototypes, descriptors, invisible text, and hostile property keys without broadening the catalogue or resolver contract.
Output: Fail-first regressions in `test/suggestions.test.ts` and a narrow descriptor/text/path repair in `src/suggestions.ts`.
</objective>

<execution_context>
@/Users/flo/.codex/gsd-core/workflows/execute-plan.md
@/Users/flo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-VERIFICATION.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-REVIEW.md
@.planning/phases/03-catalogue-mechanism-bootstrap/03-03-SUMMARY.md
@src/suggestions.ts
@test/suggestions.test.ts

<interfaces>
Existing public production contracts remain unchanged:

- `validateSuggestionCatalogue(input: unknown): ValidationResult`
- `CatalogueIssueCode` remains the existing twelve-code union.
- `CatalogueIssue` retains `code`, `path`, `value`, `expected`, and optional contextual fields.
- `Suggestion`, `ResolvedSuggestion`, `suggestionCatalogue`, and `getSuggestionsForBank()` retain their existing shapes and semantics.
- Diagnostic stage order remains missing fields, known-field validity, unexpected own fields, indexed steps, then valid-record-only duplicate checks.

No new public production symbol is needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-first public validator regressions</name>
  <files>test/suggestions.test.ts</files>
  <read_first>
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-VERIFICATION.md` frontmatter `gaps` plus Behavioral Spot-Checks for the four authoritative reproduced failures.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-REVIEW.md` findings CR-01 and WR-01 through WR-03 for exact adversarial fixture shapes and expected repair boundaries.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md` for D-02 and D-06 through D-08; keep the five-field, pure, staged, actionable contract fixed.
    - `test/suggestions.test.ts` for `validEntry`, exact issue-object assertions, the existing hidden-own-key tests, and the 46-test green baseline.
    - `src/suggestions.ts` for the public validator, existing issue-code union, exact expected-rule strings, and current stage order; do not test an internal helper.
  </read_first>
  <behavior>
    - A sparse catalogue slot with an inherited valid record and a sparse steps slot with an inherited canonical key both report the existing issue at the physical slot with `value: undefined`; separate throwing-accessor fixtures at a top-level catalogue index and a steps index return the exact `entry-object` and `invalid-step` issues, call neither getter, and let no exception escape.
    - Inherited allowed fields remain missing, and own accessors on allowed or unexpected fields produce ValidationResult issues without invoking or throwing from their getters.
    - IDs and labels made only from Unicode whitespace, format, or control code points fail their existing field-specific blank-text diagnostics; a string containing visible Unicode remains verbatim.
    - Unsafe string keys containing ordinary newlines, U+2028, or U+2029 produce exact escaped bracket paths with no raw line separators; symbols whose shared description contains U+2028 and U+2029 produce exact, distinct ordinal-bearing paths in deterministic own-key order.
  </behavior>
  <action>
Extend the public validation describe block with six focused regressions named `rejects inherited sparse slots instead of projecting prototype values`, `rejects a top-level accessor slot without invoking its getter`, `rejects a steps accessor slot without invoking its getter`, `rejects inherited and accessor record fields without invoking getters`, `rejects format and control only ids and labels`, and `encodes unsafe string and same-description symbol paths unambiguously`.

For inherited array slots, allocate holes at known top-level and step indexes, attach custom prototypes that provide an otherwise-valid record or canonical key at those numeric properties, and assert the validator sees `undefined` through the existing `entry-object` or `invalid-step` issue at the exact index. Add two independent accessor-backed numeric-slot fixtures rather than sharing one path: index 0 of a one-slot catalogue and index 1 inside an otherwise-valid three-slot `steps` array. Give each fixture its own getter counter and throwing getter, capture the public validator call under an explicit no-throw assertion, then pin the entire returned issue array. The catalogue case must return exactly `entry-object` at `$[0]` with `value: undefined` and the existing `a non-null object` expectation; the steps case must return exactly `invalid-step` at `$[0].steps[1]` with `entryId: 'fixture-one'`, `bankIndex: 1`, `value: undefined`, and the existing canonical-pad-key expectation. Assert both counters remain zero.

For record descriptors, cover an allowed field supplied only by the prototype, each of the five allowed own fields defined as a throwing accessor, and an unexpected accessor key. Assert inherited data does not leak into issue context, accessors are not called, no exception escapes, and existing codes remain field-specific: `invalid-id`, `invalid-bank-index`, `invalid-label`, `invalid-kind`, or `invalid-steps`; the unexpected accessor remains `unexpected-field` with `value: undefined`. Pin exact issue arrays so missing-field, known-field, unexpected-field, step, and duplicate stages cannot move.

For authored text, add table cases for IDs and labels containing only U+200B, control-only input, and mixed whitespace/format/control input. Keep the existing `TEXT_RULE` expectation stable. Also assert a visible Unicode label containing a format code point is accepted and preserved byte-for-byte, because the rule requires at least one visible code point rather than normalization.

Replace the existing single-symbol path case with one combined exact-path regression. Define unexpected string keys in this insertion order: `safeExtra`, `with.dot`, `with[brackets]`, `with"quote`, `with\nnewline`, `with<U+2028>separator`, and `with<U+2029>separator`, where angle-bracketed code-point names mean the actual character in the fixture. Pin their ordered paths exactly as `$[0].safeExtra`, `$[0]["with.dot"]`, `$[0]["with[brackets]"]`, `$[0]["with\"quote"]`, `$[0]["with\nnewline"]`, `$[0]["with\u2028separator"]`, and `$[0]["with\u2029separator"]`. Assert no path contains a raw CR, LF, U+2028, or U+2029 code point. Then add two distinct symbols whose shared description is the actual string `hidden<U+2028><U+2029>`; after the five allowed fields and seven string extras, their exact zero-based `Reflect.ownKeys` ordinal paths must be `$[0][#12:Symbol(hidden\u2028\u2029)]` and `$[0][#13:Symbol(hidden\u2028\u2029)]` in that order. Values must come from data descriptors only.

Run the focused suite before production changes. The structured report must show exactly these six regression titles failing and every other assertion passing; collection, import, syntax, or unrelated failures do not satisfy RED. Do not modify source or catalogue data in this task.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process'); const expected=['rejects inherited sparse slots instead of projecting prototype values','rejects a top-level accessor slot without invoking its getter','rejects a steps accessor slot without invoking its getter','rejects inherited and accessor record fields without invoking getters','rejects format and control only ids and labels','encodes unsafe string and same-description symbol paths unambiguously']; const r=spawnSync('./node_modules/.bin/vitest',['run','test/suggestions.test.ts','--reporter=json'],{encoding:'utf8'}); process.stderr.write(r.stderr||''); let report; try{report=JSON.parse(r.stdout)}catch{process.stdout.write(r.stdout||''); throw new Error('Vitest did not produce structured results')} const assertions=(report.testResults||[]).flatMap(result=&gt;result.assertionResults||[]); const failed=assertions.filter(test=&gt;test.status==='failed'); if(r.status===0||report.success!==false||failed.length!==6) throw new Error('RED requires exactly six intended failures'); for(const title of expected){const matches=assertions.filter(test=&gt;test.title===title); if(matches.length!==1||matches[0].status!=='failed') throw new Error('missing intended failure: '+title)} const others=assertions.filter(test=&gt;!expected.includes(test.title)); if(others.length&lt;45||others.some(test=&gt;test.status!=='passed')) throw new Error('an existing regression is not green'); console.log('RED verified: six intended public-validator regressions fail; '+others.length+' other assertions pass')"</automated>
  </verify>
  <acceptance_criteria>
    - All four verifier findings are reproduced through `validateSuggestionCatalogue`, not through private helpers.
    - Prototype values and accessor return values never appear as expected trusted projections in the tests.
    - Separate top-level catalogue-slot and steps-slot throwing accessors each return their exact existing issue with `value: undefined`; independent counters remain zero and explicit no-throw assertions prove neither exception escapes.
    - Getter counters and no-throw assertions prove allowed and unexpected record-field accessors also remain unexecuted.
    - Text cases cover whitespace, format, and control categories while retaining a visible-Unicode acceptance case.
    - Unsafe string paths containing ordinary newlines, U+2028, and U+2029, plus duplicate-description symbol paths containing U+2028 and U+2029, are asserted as exact, escaped, deterministic, single-line strings; symbol paths are distinct by ordinal.
    - The focused suite is red only on the six named regressions, and `src/suggestions.ts` is unchanged.
  </acceptance_criteria>
  <done>The green-suite blind spots are executable fail-first contracts at the same public unknown boundary the verifier probed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair descriptor reads, authored text, and diagnostic paths</name>
  <files>src/suggestions.ts</files>
  <read_first>
    - `test/suggestions.test.ts` as modified by Task 1 for exact issue values, context, path encodings, ordinals, and getter-call assertions.
    - `src/suggestions.ts` for `isAuthoredText`, `issueContext`, the indexed array scans, `Reflect.ownKeys` exact-field stage, valid-entry projection, and `formatCatalogueIssue`.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-CONTEXT.md` for D-02 and D-06 through D-08; public shape, purity, stage order, and actionable diagnostics are locked.
    - `.planning/phases/03-catalogue-mechanism-bootstrap/03-03-SUMMARY.md` for dense traversal and all-own-key projection behavior that must remain intact.
  </read_first>
  <behavior>
    - Array holes, inherited numeric properties, top-level catalogue accessors, and steps accessors all enter existing validation as `undefined` without prototype lookup or getter execution; the two accessor paths retain their exact `entry-object` and `invalid-step` issue contracts.
    - Allowed record fields are missing only when no own descriptor exists; own accessors are present but invalid and never supply context or trusted values.
    - Authored text retains exact content and surrounding-whitespace rejection while requiring at least one code point outside `White_Space`, `Cf`, and `Cc`.
    - Every unexpected-key path, including string keys and symbol descriptions containing U+2028 or U+2029, is deterministically escaped, single-line, and unique within one record while issue and duplicate stage ordering stays unchanged.
  </behavior>
  <action>
Refactor the validator's unknown reads around small private descriptor helpers without changing any exported type, function, issue code, catalogue value, or resolver behavior. Read each array index through its own property descriptor; return the descriptor's `value` only for an own data property and otherwise feed `undefined` into the existing entry or pad-key checks. Apply the same rule to top-level and steps arrays so inherited numeric properties and numeric accessors cannot enter projection.

Snapshot or retrieve own descriptors for record keys without calling property access or `Reflect.get`. For each allowed field, distinguish descriptor absence from accessor presence: absence emits the existing `missing-field`; an accessor is present but yields no data value and therefore emits the existing field-specific invalid issue. Build `issueContext` only from descriptor-derived `id` and `bankIndex`. For unexpected keys, keep `Reflect.ownKeys` order, use the descriptor's value only when it is a data property, and report `undefined` for an accessor. Copy only the five validated data values into fresh projections, then preserve the existing valid-record-only duplicate pass.

Strengthen `isAuthoredText` with a Unicode-property-escape visible-code-point predicate that excludes `White_Space`, `Cf`, and `Cc`, while retaining exact-string preservation and the established surrounding-whitespace check. Keep the existing diagnostic expected-rule text and field-specific codes so this behavioral refinement does not widen the public issue vocabulary.

Centralize unexpected-key path construction. Continue dot notation only for ASCII identifier-safe strings. Encode every other string with JSON-quoted bracket notation and explicitly convert literal U+2028 and U+2029 code points, in addition to ordinary newline characters already handled by JSON quoting, to their `\u2028` and `\u2029` escape sequences. For symbols, apply the same line-separator escaping to the `String(symbol)` fragment and use the exact `[#ordinal:String(symbol)]` shape pinned by Task 1, where `ordinal` is the zero-based position from the record's `Reflect.ownKeys` sequence. This produces distinct exact paths even when descriptions match. Do not reorder keys or diagnostics.

Run the focused suite until all descriptor, text, and path regressions pass alongside the original 46-test behavior, then run `just check` and `just ci`. Leave `src/suggestions.data.json`, `src/banks.data.json`, public types, the twelve-code union, bootstrap content, canonical lookup, source ordering, and import-time failure formatting semantics unchanged. Comments, if needed, explain only why own data descriptors are the trust boundary.
  </action>
  <verify>
    <automated>npm test -- test/suggestions.test.ts &amp;&amp; just check &amp;&amp; just ci</automated>
  </verify>
  <acceptance_criteria>
    - Inherited, missing, top-level accessor, and steps accessor array slots return exact existing issues with `value: undefined`; each throwing getter has zero calls and neither exception escapes.
    - Inherited and accessor record fields return deterministic `ValidationResult` failures, never escape as exceptions, and never leak prototype/accessor values into issue context.
    - U+200B-only, control-only, and mixed invisible IDs/labels fail while visible Unicode remains verbatim.
    - Unsafe string paths containing ordinary newlines, U+2028, or U+2029 contain exact escaped bracket keys with no raw line separators; symbol descriptions receive the same escaping and same-description symbols have distinct stable ordinal paths in deterministic order.
    - The complete pre-existing validation, duplicate, bootstrap, canonical-label, deterministic lookup, supported-kind, fresh-identity, and 98-empty-bank contract remains green.
    - Both catalogue data files are unchanged, and no public export or issue-code union member is added or removed.
  </acceptance_criteria>
  <done>The public validator is descriptor-safe, visually honest, exception-contained, and diagnostically unambiguous without changing Phase 3's shipped data or resolver contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- New production files: none.
- New test files: none.
- New public production symbols: none.
- Modified `test/suggestions.test.ts`
  - Six fail-first public-validator regressions covering the four verifier findings.
- Modified `src/suggestions.ts`
  - Private own-data descriptor reads for arrays and records.
  - Private visible-text predicate and stable unexpected-key path encoding.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JavaScript `unknown` values to typed suggestions | Arrays and records may carry prototypes, holes, accessors, invisible text, symbols, and unsafe strings that plain JSON does not express; the exported validator must still fail closed. |
| Malformed property keys to repair diagnostics | Locally authored values cross into single-line issue paths and import-time error text used by humans and automation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-G5 | Tampering | Array-slot and record-field reads in `validateSuggestionCatalogue` | high | mitigate | Inspect own descriptors only, accept data-property values only, reject missing/accessor slots through existing issues, and prove inherited values never reach projection. |
| T-03-G6 | Denial of Service | Accessor-bearing unknown input | high | mitigate | Never invoke getters while validating allowed fields, unexpected fields, catalogue slots, or steps slots; public regressions assert no throw and zero getter calls. |
| T-03-G7 | Tampering | Authored `id` and `label` validation | medium | mitigate | Require a Unicode-visible code point in addition to the existing exact whitespace rule; test format/control-only and visible-Unicode cases. |
| T-03-G8 | Repudiation | `CatalogueIssue.path` construction | medium | mitigate | Encode unsafe strings, escape line separators, and add stable own-key ordinals for symbols so each issue has one unambiguous single-line location. |
| T-03-G9 | Elevation of Privilege | Validated projection and resolver boundary | medium | mitigate | Preserve exact five-field copying, valid-record-only duplicates, fresh projections, canonical resolution, and inert data-only output under the full regression suite. |
| T-03-SC | Tampering | Package supply chain | low | accept | No package or lockfile change is needed; the repair uses standard ECMAScript descriptors, Unicode regex properties, and existing Vitest tooling. |
</threat_model>

<verification>
- Task 1 machine-proves RED for exactly six named regressions covering the four authoritative verifier gaps through the public validator.
- `npm test -- test/suggestions.test.ts` proves the repaired boundary and all established Phase 3 behavior together.
- `just check` proves strict TypeScript/Svelte correctness without public-type widening.
- `just ci` proves the complete test suite and production build remain green.
- `git diff -- src/suggestions.data.json src/banks.data.json` remains empty.
- Exactly 16 spec-less edge rows remain visibly represented as flagged assumptions; none is dismissed or silently dropped.
- The three non-authoritative prohibitions remain descriptor-less, flagged, and unverified.
- No external API integration exists in scope, so no `COVERAGE.md` is created.
</verification>

<success_criteria>
- The verifier's inherited-slot, accessor, invisible-text, and ambiguous-path probes all return deterministic `ValidationResult` failures without side effects or thrown getters.
- The focused regression suite fails before and passes after the narrow production repair.
- Every previously verified Phase 3 behavior remains green under `just ci`.
- Phase 3 retains exactly one catalogue, three bootstrap records, canonical naming, deterministic source-order lookup, two supported kinds, and 98 empty banks.
- No catalogue content, canonical data, public API, issue-code member, diagnostic stage, dependency, UI, MIDI, engine, timing, state, storage, or network surface changes.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature or requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | — | Trustworthy bank-aware suggestion maintenance without application-code edits for content changes | 03-01, 03-02, 03-03, 03-04 | COVERED | Plan 04 closes the remaining reproduced trust-boundary failures; the JSON maintenance surface remains unchanged. |
| REQ | PROG-01 | One plain agent-editable catalogue | 03-01, 03-02 | COVERED | Already verified; Plan 04 changes neither authoring surface nor catalogue content. |
| REQ | PROG-02 | Factory bank plus ordered valid pad keys | 03-01, 03-02, 03-03, 03-04 | COVERED | Plan 04 prevents inherited or accessor slots from becoming authored keys. |
| REQ | PROG-03 | Reject invalid, blank, duplicate, and malformed entries with actionable errors | 03-01, 03-02, 03-03, 03-04 | COVERED | Plan 04 covers descriptors, invisible text, and hostile diagnostic keys. |
| REQ | PROG-04 | Resolve names from canonical bank data | 03-01, 03-02 | COVERED | Already verified; catalogue and resolver remain unchanged. |
| REQ | PROG-05 | Deterministic zero/one/many lookup | 03-01, 03-02 | COVERED | Already verified and retained by the full focused suite. |
| REQ | PROG-06 | Preserve progression and movement kinds | 03-01, 03-02 | COVERED | Already verified; accessor-backed kinds now fail without changing supported values. |
| REQ | PROG-07 | Automated integrity and resolution proof | 03-01, 03-02, 03-03, 03-04 | COVERED | Plan 04 adds fail-first public regressions for all four current verifier findings. |
| REQ | BOOT-01 | Exact tiny bootstrap and honest empty banks | 03-01, 03-02 | COVERED | Already verified; data files remain untouched and full CI rechecks all 100 banks. |
| RESEARCH | — | Treat imported and public input as `unknown` before fresh known-field projection | 03-02, 03-03, 03-04 | COVERED | Descriptor-only reads complete the declared JavaScript unknown boundary. |
| RESEARCH | — | Fixed staged diagnostics and valid-record-only duplicate checks | 03-01, 03-02, 03-03, 03-04 | COVERED | Plan 04 explicitly preserves the stage sequence and existing duplicate pass. |
| RESEARCH | — | Canonical resolution, fallback labels, and source-array ordering | 03-01, 03-02 | COVERED | Already verified; resolver semantics remain outside the repair. |
| RESEARCH | — | Test synthetic malformed inputs rather than only production JSON | 03-01, 03-03, 03-04 | COVERED | Plan 04 adds the independently reproduced JavaScript-only fixtures. |
| RESEARCH | — | No package installation; Vitest plus `just ci` | 03-01, 03-02, 03-03, 03-04 | COVERED | Existing locked tooling only; supply-chain audit remains not triggered. |
| RESEARCH | — | Copy validated known fields only; never expose caller or canonical mutable objects | 03-02, 03-03, 03-04 | COVERED | Descriptor values enter the same fresh five-field projection protected by identity tests. |
| CONTEXT | D-01 | One flat JSON catalogue | 03-01, 03-02 | COVERED | Existing artifact unchanged. |
| CONTEXT | D-02 | Exactly five record fields, two kinds, and ordered canonical-key steps | 03-01, 03-02, 03-03, 03-04 | COVERED | Descriptor checks refine exact-field enforcement without adding fields or kinds. |
| CONTEXT | D-03 | Numeric 1..100 bank identity and canonical names only | 03-01, 03-02 | COVERED | Already verified and untouched. |
| CONTEXT | D-04 | Array order is suggestion order; no order field | 03-01, 03-02 | COVERED | Already verified; indexed own-slot reads retain positions. |
| CONTEXT | D-05 | No speculative metadata | 03-01, 03-02, 03-03 | COVERED | All-own-key rejection remains intact. |
| CONTEXT | D-06 | Pure non-mutating validation | 03-01, 03-02, 03-03, 03-04 | COVERED | Getter non-invocation closes the remaining purity and exception-containment gap. |
| CONTEXT | D-07 | Staged complete deterministic validation | 03-01, 03-02, 03-03, 03-04 | COVERED | Descriptor and path cases join existing stages without reordering. |
| CONTEXT | D-08 | Actionable contextual diagnostics | 03-01, 03-02, 03-03, 03-04 | COVERED | Unsafe strings and same-description symbols gain stable single-line locations. |
| CONTEXT | D-09 | Mechanical repair without unnecessary escalation | 03-01, 03-02, 03-04 | COVERED | The reproduced failures have deterministic repairs and require no curation decision. |
| CONTEXT | D-10 | Exactly three records across two banks | 03-01, 03-02 | COVERED | Existing data remains untouched. |
| CONTEXT | D-11 | Exact ordered Bank 1 progressions | 03-01, 03-02 | COVERED | Existing exact tests remain in the focused gate. |
| CONTEXT | D-12 | Exact Bank 14 movement and fallback path | 03-01, 03-02 | COVERED | Existing exact tests remain in the focused gate. |
| CONTEXT | D-13 | Other 98 banks resolve empty | 03-01, 03-02 | COVERED | Existing exhaustive test remains in the focused gate. |
| CONTEXT | D-14 | Fixtures make no comprehensive-authority claim | 03-01, 03-02 | COVERED | No content or authoring workflow is added. |

Spec-less fallback audit: 16 applicable edge rows, 0 resolved, 16 unresolved. All 16 appear once in `must_haves.assumptions` with `status: unresolved` and `flagged: true`; no row is auto-dismissed. The three surviving PROG-01, PROG-04, and BOOT-01 prohibitions remain visible in `must_haves.prohibitions` without any wired-check descriptor.

Deferred catalogue expansion, Phase 4 suggestion rendering, Phase 5 variation work, selector exploration, Phase 6 external BPM, and transport work remain excluded. No deferred idea appears in either task.
</source_audit>

<output>
Create `.planning/phases/03-catalogue-mechanism-bootstrap/03-04-SUMMARY.md` when done.
</output>
