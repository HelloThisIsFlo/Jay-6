---
phase: 02-post-prototype-polish-uat-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clock.ts
  - test/clock.test.ts
autonomous: true
requirements:
  - REQ-rhythm-phase-alignment-ext-clock
must_haves:
  truths:
    - "`nextDownbeatTick(currentTick)` returns the next 24-tick boundary, never the current one."
    - "Helper is unit-tested for the at-zero edge, the just-past-boundary edge, and arbitrary mid-range values."
    - "Helper is a pure function in `src/clock.ts` consumable by all three rhythm-driven engines."
  artifacts:
    - path: "src/clock.ts"
      provides: "Pure-math downbeat boundary helper for engine arm gating under Ext clock."
      contains: "export function nextDownbeatTick"
    - path: "test/clock.test.ts"
      provides: "Boundary-case coverage for nextDownbeatTick."
      contains: "describe('nextDownbeatTick'"
  key_links:
    - from: "src/clock.ts"
      to: "src/engines/{arp,phraseDuration,rhythmGate}.ts"
      via: "named import of nextDownbeatTick"
      pattern: "import\\s+\\{[^}]*nextDownbeatTick[^}]*\\}\\s+from\\s+['\"]\\.\\./clock['\"]"
    - from: "test/clock.test.ts"
      to: "src/clock.ts"
      via: "named import + describe block"
      pattern: "describe\\('nextDownbeatTick'"
---

<objective>
Add the pure-math helper `nextDownbeatTick(currentTick: number): number` to `src/clock.ts` and the Vitest cases that pin its boundary semantics. This is the testable surface of REQ-rhythm-phase-alignment-ext-clock per DEC-tests-data-and-math-only — the engine-side wiring lands in plan 02-04 (Wave 2).

Purpose: Per D-06 the first rhythm step under Ext clock must land on `tick % 24 == 0`. The helper centralizes the boundary math so all three rhythm engines (Arp, PhraseDuration, RhythmGate) share one verified implementation rather than re-deriving the formula three times. Pitfall 3 in RESEARCH.md flags `nextDownbeatTick(0)` as the off-by-one risk — tests cover that explicitly.

Output: One pure exported helper in `src/clock.ts` and one new `describe` block in `test/clock.test.ts`. No engine code touched in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
@src/clock.ts
@test/clock.test.ts

<interfaces>
Existing in `src/clock.ts`:
- `export const TICKS_PER_QUARTER = 24` — reuse, do NOT redeclare the literal `24`.
- `export function ticksPerStep(duration: PhraseDuration, triplet: boolean): number` — sibling helper showing the pattern (explicit `number` return type, no internal state).

New export (this plan):
- `export function nextDownbeatTick(currentTick: number): number`
  - Returns the next tick value where `tick % TICKS_PER_QUARTER === 0`.
  - At `currentTick = 0` → returns `24` (NEVER returns the same tick — D-06 "first step lands on a downbeat" reads as wait-for-next, per RESEARCH.md Open Question Q2 + Pitfall 3).
  - Formula: `(Math.floor(currentTick / TICKS_PER_QUARTER) + 1) * TICKS_PER_QUARTER`.

Test imports already present in `test/clock.test.ts`:
- `import { describe, expect, it } from 'vitest';`
- `import { ..., TICKS_PER_QUARTER, ... } from '../src/clock';`
Add `nextDownbeatTick` to that existing import line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nextDownbeatTick helper + boundary tests</name>
  <files>src/clock.ts, test/clock.test.ts</files>
  <read_first>
    src/clock.ts
    test/clock.test.ts
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1 (at-zero never returns same): `nextDownbeatTick(0)` returns `24`. `nextDownbeatTick(24)` returns `48`. `nextDownbeatTick(96)` returns `120`.
    - Test 2 (partial-tick rounds up): `nextDownbeatTick(1)` returns `24`. `nextDownbeatTick(23)` returns `24`. `nextDownbeatTick(25)` returns `48`. `nextDownbeatTick(47)` returns `48`.
    - Tests live in a new sibling `describe('nextDownbeatTick', ...)` block at the end of `test/clock.test.ts` (do not merge into the existing `describe('clock', ...)` block — mirror the file's "one describe per helper family" style).
    - Comment in `src/clock.ts` above the helper is WHY-only per project convention: cite D-06 and state the boundary semantics ("at currentTick=0 returns 24, never returns currentTick itself").
  </behavior>
  <action>
    Cycle: RED first. Append the new `describe('nextDownbeatTick', ...)` block to `test/clock.test.ts` (two `it` cases per the behavior list above), extend the existing `import { ... } from '../src/clock'` line to also import `nextDownbeatTick`, run `just test` — confirm the new describe fails because the export does not yet exist. Then GREEN: add `export function nextDownbeatTick(currentTick: number): number` to `src/clock.ts` using the formula `(Math.floor(currentTick / TICKS_PER_QUARTER) + 1) * TICKS_PER_QUARTER`, with a one-line WHY comment citing D-06 + the at-zero edge per PATTERNS.md §"src/clock.ts" + RESEARCH.md Pitfall 3. Run `just test` — confirm all clock tests pass. Place the new export immediately below `ticksPerStep` so it sits in the same helper family. Use the existing `TICKS_PER_QUARTER` constant — never the literal `24`. Place comment style: project convention is decision-ID + WHY (see `src/tickSource.ts` line 74–75 and `src/engines/host.ts` line 67–69 for the established analog). Do NOT add `Math.max(0, ...)` or any input-clamping — keep the helper pure; engines pass non-negative tick counts only.
  </action>
  <verify>
    <automated>just test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/clock.ts` contains `export function nextDownbeatTick(currentTick: number): number`.
    - `src/clock.ts` body of the helper references `TICKS_PER_QUARTER` (not the literal `24`).
    - `test/clock.test.ts` contains `describe('nextDownbeatTick'` followed by at least two `it(...)` blocks covering at-zero (`nextDownbeatTick(0) === 24`) and partial-tick (`nextDownbeatTick(23) === 24`) cases.
    - `just test` exits 0; vitest output reports the new `nextDownbeatTick` describe block as fully passing.
    - `just check` (svelte-check) reports no new type errors introduced.
  </acceptance_criteria>
  <done>Pure-math helper exists and is fully covered. Plan 02-04 can import it from `../clock` to gate engine `start()` under Ext mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure function over a `number` input. No I/O, no network, no DOM, no MIDI. The function is in the Data layer of `src/clock.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering (of musical state via wrong math) | `nextDownbeatTick` boundary formula | mitigate | Unit tests pin the at-zero edge (`(0) === 24`) and the just-past-boundary edge (`(24) === 48`) — the exact off-by-one risk flagged in RESEARCH.md Pitfall 3. ASVS V11 (business logic) addressed by deterministic test cases. |
| T-02-02-02 | Information Disclosure | clock.ts | accept | Pure math; no PII, no secrets, no MIDI device data crosses this boundary. |
</threat_model>

<verification>
- `just test` passes (vitest).
- `just check` passes (svelte-check — type sanity).
- `grep -E "export function nextDownbeatTick" src/clock.ts` matches one line.
- `grep -E "describe\\('nextDownbeatTick'" test/clock.test.ts` matches one line.
</verification>

<success_criteria>
The boundary helper is implemented + tested + green. The 02-04 wiring plan can `import { nextDownbeatTick } from '../clock'` and use it in engine `start()` under Ext mode without re-deriving the math.
</success_criteria>

<output>
Create `.planning/phases/02-post-prototype-polish-uat-acceptance/02-02-SUMMARY.md` when done. Include the vitest output line count for the new `nextDownbeatTick` describe block (should be at least 2 passing tests).
</output>
