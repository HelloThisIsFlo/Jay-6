---
phase: 02-post-prototype-polish-uat-acceptance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TopBar.svelte
  - src/components/PianoLayout.svelte
  - src/App.svelte
autonomous: true
requirements:
  - REQ-ipad-polish
must_haves:
  truths:
    - "Long-press on any TopBar control no longer triggers iOS text selection."
    - "TopBar buttons + segmented controls are at least 44×44 CSS px (Apple HIG)."
    - "Tap delay on TopBar buttons is gone (`touch-action: manipulation`)."
    - "On iPad the viewport does not rubber-band scroll when starting a press mid-screen."
    - "Black pads on the dark piano frame are readably distinguishable (UI-SPEC Option B fill + 1px inset top highlight)."
    - "Every interactive TopBar control has a visible `:active` state on tap (touch substitute for `:hover`)."
  artifacts:
    - path: "src/components/TopBar.svelte"
      provides: "iPad CSS contract — user-select:none, touch-action:manipulation, 44pt min, :active feedback."
      contains: "user-select: none"
    - path: "src/components/PianoLayout.svelte"
      provides: "Black-key visibility fix (UI-SPEC Option B)."
      contains: "background: #2e2e2e"
    - path: "src/App.svelte"
      provides: "iPad body scroll lock under `@media (pointer: coarse) and (max-width: 1366px)`."
      contains: "overscroll-behavior: none"
  key_links:
    - from: "src/components/TopBar.svelte"
      to: ".planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md"
      via: "Interaction Contract — mechanical CSS rules"
      pattern: "touch-action: manipulation"
    - from: "src/components/PianoLayout.svelte"
      to: ".planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md"
      via: "Color §Black-key visibility fix — Option B"
      pattern: "box-shadow: inset 0 1px 0"
---

<objective>
Ship the iPad ergonomics pass (REQ-ipad-polish per D-08) and the black-key visibility fix (D-10 Option B) per the locked UI-SPEC. Mechanical CSS changes only — no JS, no new dependencies, no component restructure.

Purpose: Phase 1 left iPad usability raw. Long-press triggers iOS text selection; tap latency is 300ms; pads can land on rubber-band scroll; black keys disappear into the dark frame. This plan closes all four with the smallest CSS diff per the UI-SPEC contract.

Output: Three modified files. Body scroll lock lives in `App.svelte` (must be at app root for `:global(html), :global(body)`). TopBar gets the touch contract. PianoLayout black-key block gets Option B's lifted fill + 1px inset top highlight.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-CONTEXT.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
@.planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
@src/components/TopBar.svelte
@src/components/PianoLayout.svelte
@src/App.svelte

<interfaces>
This plan touches CSS only. No exported TypeScript surfaces change. Reference shapes from existing files:

`src/components/TopBar.svelte` — existing `<style>` block (lines 204–280) already defines:
- `.topbar` (root flex container)
- `.field` (label+control wrapper)
- `select, input[type='number']` (form controls)
- `.bank-row`, `.arrow` (bank prev/next buttons)
- `.transpose button` (±12 octave buttons)
- `.latch` (latch toggle button)
- `.seg button` (Int/Ext segmented buttons)

`src/components/PianoLayout.svelte` — existing `<style>` block (lines 87–147) has:
- `.pad.black` block at lines 134–139 (current fill `#1f1f1f` — Option B replaces this).
- `.pad.held` at lines 140–145 with `!important` for the J-6 orange — preserve untouched (UI-SPEC: locked).
- `.piano` already sets `user-select: none; touch-action: none` (lines 98–99) — the analog pattern this plan mirrors into TopBar.

`src/App.svelte` — existing `<style>` block (lines 138–158) is global-scoped and currently defines `main`, `footer`, `footer code`. The body scroll lock goes here because `:global(html), :global(body)` selectors are needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TopBar iPad CSS contract</name>
  <files>src/components/TopBar.svelte</files>
  <read_first>
    src/components/TopBar.svelte
    src/components/PianoLayout.svelte
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <action>
    Extend the existing `<style>` block in `src/components/TopBar.svelte`. Do NOT restructure existing rules — append. Apply per UI-SPEC §Interaction Contract + RESEARCH.md Pattern 7 + Pitfall 6:
    1. Add `user-select: none;` and `-webkit-user-select: none;` to the existing `.topbar` rule (per D-08; mirrors PianoLayout.svelte line 98).
    2. Add a new rule selecting `button, input[type='number'], input[type='range']` with `touch-action: manipulation;`. Per Pitfall 6, do NOT include `select` in this selector — leave touch-action default on `<select>` so iOS native dropdown gesture still opens.
    3. Add a new rule selecting `.arrow, .latch, .seg button, .transpose button` with `min-width: 44px;` and `min-height: 44px;` (Apple HIG). Per Pitfall 6, do NOT apply min-height globally to `button` — it breaks `<select>` option rendering.
    4. Add `:active` rules for the same four selectors with `filter: brightness(1.15);` (UI-SPEC Visual Contract — touch substitute for `:hover`).
    5. Each new CSS block carries a WHY-only comment citing the decision ID (`/* D-08: ... */`) per project comment convention.
  </action>
  <verify>
    <automated>grep -E "user-select: none" src/components/TopBar.svelte && grep -E "touch-action: manipulation" src/components/TopBar.svelte && grep -E "min-height: 44px" src/components/TopBar.svelte && grep -E "filter: brightness\\(1\\.15\\)" src/components/TopBar.svelte && just check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/TopBar.svelte` `<style>` block contains `user-select: none` on the `.topbar` rule.
    - The selector containing `touch-action: manipulation` matches `button, input[type='number'], input[type='range']` and does NOT include `select`.
    - The selector applying `min-width: 44px` and `min-height: 44px` matches `.arrow, .latch, .seg button, .transpose button` (per-element, not global `button`).
    - A `:active` rule with `filter: brightness(1.15)` exists for `.arrow, .latch, .seg button, .transpose button`.
    - `just check` reports zero new svelte-check errors.
  </acceptance_criteria>
  <done>TopBar CSS contract per UI-SPEC is shipped. Desktop unchanged; iPad gets text-selection suppression, no tap delay, 44pt targets, and visible :active feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Black-key visibility fix (UI-SPEC Option B)</name>
  <files>src/components/PianoLayout.svelte</files>
  <read_first>
    src/components/PianoLayout.svelte
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-PATTERNS.md
  </read_first>
  <action>
    Modify the existing `.pad.black` block in `src/components/PianoLayout.svelte` (currently `background: #1f1f1f`). Replace ONLY the `background` value with `#2e2e2e` and add `box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);` per UI-SPEC §Color "Selected: Option B." Preserve every other property in the block (`color: #eee; min-height: 110px; border-color: #2a2a2a;`). Do NOT touch `.pad.held` (lines 140–145) — UI-SPEC locks the J-6 orange + `!important`. Comment the change with the decision ID and the chosen option letter: `/* D-10 Option B: lifted fill + 1px inset top highlight */` so the executor can walk the fallback ladder (A→C→D) if the render on real iPad reads poorly without re-invoking `/gsd:ui-phase`. The UI-SPEC fallback ladder lives at `.planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md` §"Fallback ladder (executor pivot path)".
  </action>
  <verify>
    <automated>grep -E "background: #2e2e2e" src/components/PianoLayout.svelte && grep -E "box-shadow: inset 0 1px 0 rgba\\(255, 255, 255, 0\\.08\\)" src/components/PianoLayout.svelte && grep -E "background: #ff7a1a !important" src/components/PianoLayout.svelte && just check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/PianoLayout.svelte` `.pad.black` rule contains `background: #2e2e2e` (replaced `#1f1f1f`).
    - `.pad.black` rule contains `box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08)`.
    - `.pad.held` rule remains intact with `background: #ff7a1a !important` (regression check — UI-SPEC lock).
    - `just check` reports zero new svelte-check errors.
  </acceptance_criteria>
  <done>Black pads are visually lifted off the dark frame per UI-SPEC Option B; J-6 orange held-pad highlight is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: iPad body scroll lock in App.svelte global style</name>
  <files>src/App.svelte</files>
  <read_first>
    src/App.svelte
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-UI-SPEC.md
    .planning/phases/02-post-prototype-polish-uat-acceptance/02-RESEARCH.md
  </read_first>
  <action>
    Append a single `@media (pointer: coarse) and (max-width: 1366px) { ... }` block to the existing `<style>` block in `src/App.svelte`. Inside it, target `:global(html), :global(body)` with: `overflow: hidden; position: fixed; width: 100%; height: 100%; overscroll-behavior: none;` per RESEARCH.md Pattern 6 + Pitfall 7 (the `max-width: 1366px` guard excludes touchscreen Windows laptops). This MUST live in `App.svelte` (not TopBar) because `:global()` selectors targeting `html`/`body` need to be at the app root. Add a WHY-only comment citing `D-08` and naming the pitfall guard ("D-08: iPad body scroll lock — `max-width` guard excludes large touchscreen laptops per Pitfall 7"). Do NOT modify any other `<script>` content in App.svelte — plan 02-04 (Wave 2) owns the transport-sync wiring there.
  </action>
  <verify>
    <automated>grep -E "@media \\(pointer: coarse\\) and \\(max-width: 1366px\\)" src/App.svelte && grep -E ":global\\(html\\), :global\\(body\\)" src/App.svelte && grep -E "overscroll-behavior: none" src/App.svelte && just ci 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.svelte` `<style>` block contains `@media (pointer: coarse) and (max-width: 1366px)`.
    - Inside that media query, `:global(html), :global(body)` selectors apply `overflow: hidden`, `position: fixed`, `width: 100%`, `height: 100%`, `overscroll-behavior: none`.
    - `src/App.svelte` `<script>` block diff is empty for this task (no JS touched here).
    - `just ci` exits 0 (svelte-check + vitest + vite build all green).
  </acceptance_criteria>
  <done>iPad body scroll lock is shipped at the app root under the iPad-shaped media query; desktop and large touchscreen laptops are unaffected. App.svelte `<script>` is left untouched for plan 02-04 to extend without conflict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser CSS engine ← UI styles | All changes are scoped CSS (or `:global(html), :global(body)` under a media query). No new JS, no new event handlers, no DOM mutation paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service (usability regression on non-iPad) | `App.svelte` body scroll lock media query | mitigate | Use compound query `@media (pointer: coarse) and (max-width: 1366px)` per RESEARCH.md Pitfall 7 — excludes touchscreen Windows laptops with mice. Desktop Chrome/Edge unaffected. |
| T-02-03-02 | Denial of Service (broken native dropdown on iPad) | `TopBar.svelte` touch-action rule | mitigate | Exclude `<select>` from the `touch-action: manipulation` selector per Pitfall 6 — keeps native iOS dropdown open gesture working. |
| T-02-03-03 | Tampering (visual identity regression) | `PianoLayout.svelte` `.pad.held` orange | mitigate | Task 2 explicitly preserves `.pad.held` with `!important`. Verify command greps for `background: #ff7a1a !important` to catch accidental removal. |
| T-02-03-04 | Information Disclosure | iPad polish CSS | accept | No data flow; no PII; static stylesheet served from the SPA. |
</threat_model>

<verification>
- `just check` passes (no svelte-check regressions).
- `just test` passes (no source changes affecting tested math/data).
- `just ci` passes (check + test + build, exercised at end of Task 3).
- Grep-based assertions in each task's verify command pass.
- UI verification deferred to UAT §18 (verify-phase) per DEC-tests-data-and-math-only and the manual-only verifications table in 02-VALIDATION.md.
</verification>

<success_criteria>
TopBar honors the UI-SPEC iPad ergonomics contract. PianoLayout ships Option B black-key treatment with fallback ladder traceability. App.svelte global style enforces body scroll lock on iPad-shaped viewports. Final visual sign-off happens in UAT §18 — if Option B reads poorly on hardware, executor walks the documented fallback ladder (A→C→D) without re-invoking `/gsd:ui-phase`.
</success_criteria>

<output>
Create `.planning/phases/02-post-prototype-polish-uat-acceptance/02-03-SUMMARY.md` when done. Record which Option (B by default, or A/C/D if you needed to walk the ladder) shipped, and note that final iPad visual verification is deferred to UAT §18.
</output>
