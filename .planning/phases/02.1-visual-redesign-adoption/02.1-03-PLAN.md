---
phase: 02.1-visual-redesign-adoption
plan: 03
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - src/components/PianoLayout.svelte
autonomous: true
requirements:
  - R3-pad-surface-redesign
  - R5-responsive-redesign-coverage
  - R6-existing-behavior-preservation
must_haves:
  truths:
    - "R3: Pads keep the existing separate 5-black/7-white J-6 hardware rows and exact key mapping."
    - "R3: Pointer capture, pointerup, pointercancel, lostpointercapture, and keyboard-driven held projection behavior are preserved."
    - "R3: Lifted bevel styling uses cream and black-key surfaces, with orange fill/glow only on `class:held` pads per D-03."
    - "R5: Pad surface remains playable across desktop, iPad-sized, and iPhone-landscape layouts per D-06."
  artifacts:
    - path: "src/components/PianoLayout.svelte"
      provides: "Lifted hardware pad surface with preserved event contract"
  key_links:
    - from: "src/App.svelte"
      to: "src/components/PianoLayout.svelte"
      via: "heldKeys projection passed as prop"
      pattern: "heldKeys={displayKeys}"
    - from: "src/components/PianoLayout.svelte"
      to: "src/banks.ts"
      via: "current bank chord labels still resolve through `labelFor`"
      pattern: "labelFor(bank, c)"
  prohibitions:
    - "Do not convert the pad layout into real-piano overlap."
    - "Do not remove pointercancel or lostpointercapture release paths."
    - "Do not use orange on non-held pads, decorative borders, black-key contrast, variation selected state, latch selected state, or future-scope states."
---

<objective>
Adopt the lifted J-6 hardware pad treatment while preserving the current pad interaction contract.

Purpose: Make the pad surface match the approved redesign without reintroducing stuck-note or visual/audio desync risks from v1 UAT.
Output: Restyled `PianoLayout.svelte` with the same mapping, callbacks, and held-state source.
</objective>

<execution_context>
@/Users/flo/.codex/gsd-core/workflows/execute-plan.md
@/Users/flo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-CONTEXT.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-RESEARCH.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-VALIDATION.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-PATTERNS.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
@.codex/skills/sketch-findings-jay-6/references/pads-and-feedback.md
@src/components/PianoLayout.svelte
@src/App.svelte
</context>

## Artifacts This Phase Produces

- `src/styles/tokens.css`: shared CSS custom properties for instrument chrome, pad surfaces, typography, radii, spacing, and semantic accents.
- `src/variationOptions.ts`: pure mapping helpers for per-style variation UI models.
- `src/components/VariationPicker.svelte`: per-style variation picker UI.
- `test/variationOptions.test.ts`: exact variation mapping coverage for representative indices.
- Modified `src/main.ts`, `src/App.svelte`, `src/components/TopBar.svelte`, and `src/components/PianoLayout.svelte`.
- Plan summaries: `.planning/phases/02.1-visual-redesign-adoption/02.1-01-SUMMARY.md` through `02.1-04-SUMMARY.md`.

<tasks>

<task type="auto">
  <name>Task 1: Restyle the pad frame and pad faces</name>
  <files>src/components/PianoLayout.svelte</files>
  <read_first>
    @.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-CONTEXT.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-PATTERNS.md
    @.codex/skills/sketch-findings-jay-6/references/pads-and-feedback.md
    @src/components/PianoLayout.svelte
  </read_first>
  <action>
    Update `PianoLayout.svelte` CSS to use token variables for the frame, cream pads, black-key pads, typography, compact radii, and 8px-grid spacing. Keep the existing `whiteKeys` and `blackKeys` arrays, two-row CSS grid, clean gap, and no-overlap J-6 layout per D-13 and D-14. Add lifted bevel treatment using top highlights, lower inner shadows, depth shadows, and active press translation without changing the DOM event contract. Chord labels must use mono or instrument-data styling from the token contract and remain readable on cream and black keys. Keep black keys dark and quiet; do not use new chroma to improve contrast.
  </action>
  <verify>
    <automated>just check</automated>
  </verify>
  <acceptance_criteria>
    - `PianoLayout.svelte` still defines exactly 7 white keys and 5 black keys with the existing key names and grid columns.
    - `.pad.white` uses the cream pad token/value and `.pad.black` uses the black-key token/value.
    - Pad labels remain present as key and chord-name spans.
    - The layout remains a separate black row above white row with a visible gap.
    - `just check` passes.
  </acceptance_criteria>
  <done>Pad surface visually matches the lifted hardware direction while retaining the current mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve pad active-state and pointer safety semantics</name>
  <files>src/components/PianoLayout.svelte</files>
  <read_first>
    @.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-CONTEXT.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-VALIDATION.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-PATTERNS.md
    @.codex/skills/sketch-findings-jay-6/references/pads-and-feedback.md
    @src/components/PianoLayout.svelte
  </read_first>
  <action>
    Keep active pad styling attached only to `.pad.held`, which is driven by `class:held={heldKeys.has(key)}` from App.svelte. Orange fill, glow, and border may appear only in this held class per D-03. Preserve `handlePointerDown`, `endPress`, the `captured` map, `setPointerCapture`, `releasePointerCapture`, `onpointerup`, `onpointercancel`, and `onlostpointercapture`. Maintain `touch-action: none` on the pad surface and selection suppression on pad text. Add responsive sizing constraints inside `PianoLayout.svelte` so desktop, iPad-sized, and iPhone-landscape layouts keep all twelve pads reachable and labels readable per D-06.
  </action>
  <verify>
    <automated>just check</automated>
  </verify>
  <acceptance_criteria>
    - `onpointerdown`, `onpointerup`, `onpointercancel`, `onlostpointercapture`, `setPointerCapture`, and `releasePointerCapture` remain in `PianoLayout.svelte`.
    - Orange token/value appears only in the `.pad.held` active-state styling inside this component.
    - `touch-action: none` remains on the pad grid or pad surface.
    - Responsive CSS constrains pad heights/labels so labels do not overlap or clip in short landscape.
    - `just check` passes.
  </acceptance_criteria>
  <done>Pad active visuals and pointer safety remain behaviorally equivalent to the accepted v1 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer/keyboard input -> pad callbacks | User input triggers MIDI notes through App.svelte and EngineHost. |
| Held projection -> active visual state | App.svelte-held projection controls whether pads show active sound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-06 | Denial of Service | `PianoLayout.svelte` pointer release paths | medium | mitigate | Task 2 preserves pointercancel and lostpointercapture release paths to avoid stuck notes/highlights. |
| T-02.1-07 | Spoofing | `.pad.held` active state | low | mitigate | Task 2 scopes orange active styling only to held pads driven by App.svelte projection. |
| T-02.1-SC | Tampering | npm installs | low | accept | No package installs are planned; Plan 03 is a Svelte/CSS component restyle. |
</threat_model>

<verification>
Plan 03 verification is complete when `just check` passes and source inspection confirms the pointer safety and active-state criteria. Full browser smoke is owned by Plan 04.
</verification>

<success_criteria>
- Twelve-pad J-6 hardware layout is retained.
- Lifted bevel pad treatment is implemented.
- Pointer release safety and orange active-state semantics are preserved.
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-adoption/02.1-03-SUMMARY.md` when done.
</output>
