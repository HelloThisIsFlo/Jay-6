---
phase: 02.1-visual-redesign-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.ts
  - src/styles/tokens.css
  - src/App.svelte
autonomous: true
requirements:
  - R1-visual-token-adoption
  - R5-responsive-redesign-coverage
  - R6-existing-behavior-preservation
must_haves:
  truths:
    - "R1: App shell uses the committed dark instrument chrome, cream pad, compact radius, 8px spacing, mono readout, and semantic accent direction from D-01, D-02, D-03, D-13, and D-14."
    - "R5: Desktop, iPad-sized, and iPhone-landscape shell spacing keeps the control surface reachable without removing the existing short-viewport scroll escape per D-06."
    - "R6: App.svelte continues to own keyboard handling, held/latched highlight projection, and EngineHost/tickSource bridge behavior."
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Shared sketch token source for the phase per D-01 and D-02"
    - path: "src/main.ts"
      provides: "Global token import"
    - path: "src/App.svelte"
      provides: "Instrument shell and quiet shortcut footer treatment"
  key_links:
    - from: "src/main.ts"
      to: "src/styles/tokens.css"
      via: "single CSS import before App mount"
      pattern: "import './styles/tokens.css'"
    - from: "src/App.svelte"
      to: "src/components/TopBar.svelte"
      via: "TopBar stays before PianoLayout in the shell"
      pattern: "<TopBar />"
    - from: "src/App.svelte"
      to: "src/components/PianoLayout.svelte"
      via: "displayKeys still drives active pad projection"
      pattern: "heldKeys={displayKeys}"
  prohibitions:
    - "No progression rail display, progression authoring, sequencer UI, or queued/countdown toast surfaces are introduced."
    - "No MIDI timing, engine lifecycle, chord data, transport behavior, or playback semantics change."
    - "Orange remains reserved for currently sounding or latched pads only; shell, footer, setup, latch, focus, and selected controls use non-orange treatments unless attached to active pad sound."
---

<objective>
Adopt the shared visual token foundation and app-shell chrome for Phase 02.1 without changing the v1 instrument contract.

Purpose: Establish the visual baseline other Wave 2 plans depend on, while keeping App.svelte as the existing behavior bridge.
Output: Shared tokens, imported once, plus app-shell and footer styling aligned with the approved Jay-6 v3 direction.
</objective>

<execution_context>
@/Users/flo/.codex/gsd-core/workflows/execute-plan.md
@/Users/flo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-CONTEXT.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-RESEARCH.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-VALIDATION.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-PATTERNS.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
@.codex/skills/sketch-findings-jay-6/SKILL.md
@.codex/skills/sketch-findings-jay-6/references/tokens.md
@.planning/sketches/themes/default.css
@src/main.ts
@src/App.svelte
</context>

## Artifacts This Phase Produces

- `src/styles/tokens.css`: shared CSS custom properties for instrument chrome, pad surfaces, typography, radii, spacing, and semantic accents.
- `src/variationOptions.ts`: pure mapping helpers for per-style variation UI models.
- `src/components/VariationPicker.svelte`: per-style variation picker UI.
- `test/variationOptions.test.ts`: exact variation mapping coverage for representative indices.
- Modified `src/main.ts`, `src/App.svelte`, `src/components/TopBar.svelte`, and `src/components/PianoLayout.svelte`.
- Plan summaries: `.planning/phases/02.1-visual-redesign-adoption/02.1-01-SUMMARY.md` through `02.1-04-SUMMARY.md`.

## Multi-Source Coverage Audit

| Source | ID | Requirement / Decision | Plan | Status |
|--------|----|------------------------|------|--------|
| GOAL | ROADMAP | Adopt the committed Jay-6 v3 visual redesign for the existing v1 feature set, without new playback behavior | 01, 02, 03, 04 | COVERED |
| REQ | R1 | Visual token adoption | 01, 04 | COVERED |
| REQ | R2 | TopBar C2 redesign | 02, 04 | COVERED |
| REQ | R3 | Pad surface redesign | 03, 04 | COVERED |
| REQ | R4 | Per-style variation pickers | 02, 04 | COVERED |
| REQ | R5 | Responsive redesign coverage | 01, 03, 04 | COVERED |
| REQ | R6 | Existing behavior preservation | 01, 02, 03, 04 | COVERED |
| RESEARCH | Stack | Keep Vite 6, Svelte 5 runes, TypeScript strict, Vitest, WEBMIDI.js v3; no new packages | 01, 02, 03, 04 | COVERED |
| RESEARCH | Pattern | Use scoped Svelte CSS plus small shared tokens where helpful | 01, 02, 03 | COVERED |
| RESEARCH | Pattern | Keep UI controls wired to existing state setters and MIDI selectors | 02, 04 | COVERED |
| RESEARCH | Pattern | Preserve PianoLayout pointer capture and release paths | 03, 04 | COVERED |
| RESEARCH | Validation | Run just check, just test, just ci, and browser smoke at desktop/iPad/iPhone-landscape | 04 | COVERED |
| CONTEXT | D-01 | Planner discretion on token structure | 01 | COVERED |
| CONTEXT | D-02 | Small shared token source, no heavy design system or UI library | 01 | COVERED |
| CONTEXT | D-03 | Orange active-pad semantics; steel only for existing secondary system status | 01, 02, 03, 04 | COVERED |
| CONTEXT | D-04 | TopBar C2 status pill and setup popover | 02 | COVERED |
| CONTEXT | D-05 | Popover open state uses same visual language | 02 | COVERED |
| CONTEXT | D-06 | Responsive usability for iPad and iPhone landscape | 01, 03, 04 | COVERED |
| CONTEXT | D-07 | BPM visible and read-only under external clock | 02, 04 | COVERED |
| CONTEXT | D-08 | Full sketch variation language | 02 | COVERED |
| CONTEXT | D-09 | Arp composed selectors | 02 | COVERED |
| CONTEXT | D-10 | Beat grid | 02 | COVERED |
| CONTEXT | D-11 | Rhythm Gate pattern tiles | 02 | COVERED |
| CONTEXT | D-12 | Exact 1..12 mappings through existing ui.variation path | 02, 04 | COVERED |
| CONTEXT | D-13 | Sketch screenshots as visual baseline | 04 | COVERED |
| CONTEXT | D-14 | Required baseline screenshots | 04 | COVERED |
| CONTEXT | D-15 | Source HTML may inform spacing; SPEC controls scope | 01, 02, 03 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared sketch token layer</name>
  <files>src/styles/tokens.css, src/main.ts</files>
  <read_first>
    @.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-CONTEXT.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
    @.codex/skills/sketch-findings-jay-6/references/tokens.md
    @.planning/sketches/themes/default.css
    @src/main.ts
  </read_first>
  <behavior>
    - Token file exposes the UI-SPEC palette: bg-0 through bg-5, fg-0 through fg-3, accent, accent-soft, system, system-soft, cream, black-key, frame.
    - Token file exposes typography values for mono font, 11px eyebrow, 14px body, 18px readout, 28px display.
    - Token file exposes compact radii 4px, 6px, 10px and spacing values 4px, 8px, 16px, 24px, 32px, 48px.
  </behavior>
  <action>
    Create `src/styles/tokens.css` per D-01 and D-02 by translating `.planning/sketches/themes/default.css` and UI-SPEC values into `:root` CSS custom properties. Include semantic comments only where they prevent misuse: `--accent` is active pad sound only per D-03; `--system` is for existing secondary system status. Import the token file once from `src/main.ts` before mounting `App`. Do not add any package, registry block, Tailwind, shadcn, Radix, Base UI, or component-library setup.
  </action>
  <verify>
    <automated>just check</automated>
    <automated>just test</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/tokens.css` exists and contains the exact palette values `#0e0e0e`, `#171717`, `#1f1f1f`, `#2a2a2a`, `#3a3a3a`, `#4a4a4a`, `#ff7a1a`, `#f4f1ea`, and `#2e2e2e`.
    - `src/main.ts` imports `./styles/tokens.css` exactly once.
    - `just check` and `just test` pass without new dependencies.
  </acceptance_criteria>
  <done>Shared token foundation is available to all Svelte components without changing runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the app shell around the instrument surface</name>
  <files>src/App.svelte</files>
  <read_first>
    @.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-PATTERNS.md
    @.codex/skills/sketch-findings-jay-6/references/tokens.md
    @src/App.svelte
  </read_first>
  <action>
    Update only `App.svelte` markup/CSS for the app shell and retained shortcut/footer affordance. Use token variables for background, text, footer, code chips, and spacing so the page reads as compact hardware instrument chrome per D-13, D-14, and D-15. Preserve the existing `<TopBar />` then `<PianoLayout ... heldKeys={displayKeys} />` order, all keyboard handlers, all `$effect` bridges, the MIDI subscription behavior, `displayKeys`, `press`, `release`, `clearAllHighlights`, and the coarse-pointer plus short-landscape scroll media queries per D-06. Keep footer text subordinate and non-competing; do not add any new UI surface.
  </action>
  <verify>
    <automated>just check</automated>
  </verify>
  <acceptance_criteria>
    - `App.svelte` still contains `host.setStyle(ui.style, ui.variation)`, `tickSource.setMode(ui.clockSource)`, `heldKeys={displayKeys}`, `onKeyDown`, and `onKeyUp`.
    - The short-landscape media query with `max-height: 480px` remains present.
    - Footer/shortcut affordance remains present if retained, uses quieter token-based styling, and does not introduce progression, sequencer, queued/countdown, or new playback controls.
    - `just check` passes.
  </acceptance_criteria>
  <done>App shell adopts the redesign baseline while preserving existing v1 bridge and input behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser user input -> Svelte UI | Keyboard/pointer events enter App.svelte and must keep existing behavior routing. |
| CSS tokens -> visual semantics | Shared token names can cause misleading state if orange is reused outside active pads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01 | Tampering | `src/App.svelte` behavior bridge | medium | mitigate | Task 2 forbids changes to keyboard handlers, EngineHost/tickSource `$effect` bridges, and held/latched projection; `just check` catches compile drift. |
| T-02.1-02 | Spoofing | Visual state colors | low | mitigate | Task 1 documents `--accent` as active-pad-only and Task 2 keeps shell/footer states non-orange per D-03. |
| T-02.1-SC | Tampering | npm installs | low | accept | No package installs are planned; research says no new dependency is needed, so package legitimacy risk does not cross a new trust boundary. |
</threat_model>

<verification>
Plan 01 verification is complete when `just check` and `just test` pass after token import and shell styling. Full phase verification, including `just ci` and browser smoke, is owned by Plan 04.
</verification>

<success_criteria>
- Shared tokens exist and are imported once.
- App shell and footer use the redesign token direction.
- App.svelte behavior bridge, keyboard shortcuts, highlight projection, and responsive scroll escape are unchanged in behavior.
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-adoption/02.1-01-SUMMARY.md` when done.
</output>
