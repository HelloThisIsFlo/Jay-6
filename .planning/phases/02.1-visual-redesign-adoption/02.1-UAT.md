---
status: testing
phase: 02.1-visual-redesign-adoption
source:
  - 02.1-VERIFICATION.md
started: 2026-07-06T15:37:06Z
updated: 2026-07-06T15:37:06Z
---

# Phase 02.1 UAT

## Current Test

number: 2
name: Setup and MIDI reachability
expected: |
  Open the setup pill in a real browser and exercise Output, Input, Channel,
  Clock, and BPM, including an external-clock-capable input if available. All
  setup controls should be reachable and dismissible; BPM should remain visible
  and become read-only when external clock is active.
awaiting: user response

## Tests

### 1. Visual and responsive match

expected: |
  Compare desktop, iPad-sized, and iPhone-landscape browser views against the
  committed v3 sketch direction. Dark instrument chrome, C2 TopBar, lifted pad
  surface, and per-style variation controls should align with the approved
  sketches, with no clipped controls, unreadable primary labels, or inaccessible
  pad rows.
result: passed
notes: |
  Verified via quick task 260706-nbq using live Playwright browser screenshots
  (not just source audit) at 1440x900, 1024x768, and 844x390. TopBar closed
  state, setup popover, pad surface, and Arp/Beat/Rhythm-Gate-4 variation
  pickers compared against .research/design/screenshots/{01,02,04,05,06}*.png.
  Found and fixed a real width/alignment/overflow regression chain in
  TopBar.svelte (see 260706-nbq-SUMMARY.md). No clipping, no unreadable labels,
  all pad rows reachable at all three viewports after the fix.

### 2. Setup and MIDI reachability

expected: |
  Open the setup pill in a real browser and exercise Output, Input, Channel,
  Clock, and BPM, including an external-clock-capable input if available. All
  setup controls should be reachable and dismissible; BPM should remain visible
  and become read-only when external clock is active.
result: pending

### 3. Pad browser behavior

expected: |
  Exercise pointer, keyboard, and latch pad interactions. Pad highlight should
  follow current sound/latch state exactly; orange should appear only on sounding
  or latched pads.
result: pending
notes: |
  Partially exercised during 260706-nbq: confirmed via Playwright
  pointerdown/pointerup on a live pad that orange appears only while held (D-03),
  matching mock 04. Keyboard and latch interaction paths were not re-exercised
  this session (no CSS changes touched those paths) — still needs a full pass.

### 4. Variation browser behavior

expected: |
  Exercise variation picker equivalents for indices 1, 6, 7, and 12 in Arp,
  Beat, Rhythm Gate 4, and Rhythm Gate 5. Each selection should update through
  the existing immediate ui.variation path and no queued/countdown behavior
  should appear.
result: pending
notes: |
  Partially exercised during 260706-nbq: visually confirmed Arp/Beat/Rhythm-Gate-4
  picker structure and selection styling against mocks 02/05/06, but did not
  specifically exercise indices 1/6/7/12 or Rhythm Gate 5 — still needs a full
  interaction pass.

## Summary

total: 4
passed: 1
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
