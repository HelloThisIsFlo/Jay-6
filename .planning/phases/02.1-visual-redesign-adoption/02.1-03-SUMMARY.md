---
phase: 02.1-visual-redesign-adoption
plan: 03
subsystem: ui
tags: [svelte, piano-layout, pad-surface, pointer-safety, responsive-css]

requires:
  - phase: 02.1-01
    provides: shared visual tokens and token-based app shell chrome
  - phase: 02.1-02
    provides: TopBar C2 and non-orange selected-control treatment
provides:
  - Lifted J-6 hardware pad surface using shared visual tokens
  - Preserved 5-black/7-white two-row pad mapping
  - Held-only active orange pad styling
  - Responsive pad sizing constraints for desktop, iPad-sized, and short landscape layouts
affects: [02.1-04-browser-smoke, pad-pointer-safety, visual-redesign]

tech-stack:
  added: []
  patterns:
    - Scoped Svelte CSS restyle over unchanged pad DOM and callbacks
    - Held-state projection remains the only active pad visual source
    - Component-local responsive CSS constraints for short landscape reachability

key-files:
  created:
    - .planning/phases/02.1-visual-redesign-adoption/02.1-03-SUMMARY.md
  modified:
    - src/components/PianoLayout.svelte

key-decisions:
  - "Restyle the existing two-row J-6 hardware pad component in place instead of changing DOM structure or event wiring."
  - "Use shared token variables for frame, cream pad, black-key, typography, radius, and spacing values."
  - "Keep orange active styling scoped to `.pad.held`, driven by App.svelte held/latch projection."

patterns-established:
  - "Pad bevel depth uses scoped CSS shadows on `.pad.white` and `.pad.black`; pointer behavior remains untouched."
  - "Short-landscape pad reachability is handled inside `PianoLayout.svelte` through media queries and label wrapping."

requirements-completed:
  - R3-pad-surface-redesign
  - R5-responsive-redesign-coverage
  - R6-existing-behavior-preservation

coverage:
  - id: D1
    description: "PianoLayout keeps the exact twelve-pad J-6 hardware mapping with 7 white keys, 5 black keys, separate rows, visible row gap, and existing key/chord label spans."
    requirement: R3-pad-surface-redesign
    verification:
      - kind: other
        ref: "source inspection: literal white/black key counts, grid rows, row gap, key/name spans"
        status: pass
      - kind: other
        ref: "just check"
        status: pass
    human_judgment: false
  - id: D2
    description: "Pad faces use shared cream and black-key tokens with lifted bevel treatment while keeping the black-key row dark and quiet."
    requirement: R3-pad-surface-redesign
    verification:
      - kind: other
        ref: "source inspection: `.pad.white { background: var(--cream) }` and `.pad.black { background: var(--black-key) }`"
        status: pass
      - kind: other
        ref: "just check"
        status: pass
    human_judgment: true
    rationale: "Exact visual fidelity to the sketch and real viewport feel are owned by Plan 04 browser smoke."
  - id: D3
    description: "Pointer capture, pointerup, pointercancel, lostpointercapture, held projection, and touch-action safety paths remain present."
    requirement: R6-existing-behavior-preservation
    verification:
      - kind: other
        ref: "source inspection: onpointerdown/onpointerup/onpointercancel/onlostpointercapture/setPointerCapture/releasePointerCapture/class:held/touch-action"
        status: pass
      - kind: other
        ref: "just check"
        status: pass
    human_judgment: false
  - id: D4
    description: "Orange active-pad styling is confined to `.pad.held`, and responsive CSS constrains pad height and label wrapping for compact and short landscape layouts."
    requirement: R5-responsive-redesign-coverage
    verification:
      - kind: other
        ref: "source inspection: no accent/orange references outside `.pad.held`; max-width and max-height media queries present"
        status: pass
      - kind: other
        ref: "just check"
        status: pass
    human_judgment: true
    rationale: "No-overlap and reachability across desktop, iPad-sized, and iPhone-landscape viewports require Plan 04 browser screenshots/smoke."

duration: 5min
completed: 2026-07-06
status: complete
---

# Phase 02.1 Plan 03: Lifted Pad Surface Summary

**Lifted tokenized J-6 pad surface with exact two-row key mapping, held-only orange state, and preserved pointer release safety.**

## Performance

- **Duration:** 5min
- **Started:** 2026-07-06T15:00:20Z
- **Completed:** 2026-07-06T15:04:16Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Restyled `PianoLayout.svelte` with tokenized frame, cream pad, black-key, mono label, compact radius, and 8px-grid spacing values.
- Added lifted bevel treatment through top highlights, lower inner shadows, depth shadows, and active press translation.
- Preserved the existing separate black-row-over-white-row J-6 hardware layout with exact key arrays, grid columns, key labels, and chord labels.
- Preserved pointer safety paths and held projection: pointer capture/release/cancel/lost-capture remain intact, and orange active visuals remain attached only to `.pad.held`.
- Added responsive constraints for compact and short-landscape layouts so pad labels wrap instead of overlapping and pad rows stay reachable.

## Task Commits

1. **Task 1: Restyle the pad frame and pad faces** - `16f4aa3` (feat)
2. **Task 2: Preserve pad active-state and pointer safety semantics** - `65a7682` (feat)

## Files Created/Modified

- `src/components/PianoLayout.svelte` - Tokenized lifted pad surface, held-only active styling, pointer-safe responsive pad layout.
- `.planning/phases/02.1-visual-redesign-adoption/02.1-03-SUMMARY.md` - Plan execution summary.

## Verification

- **Task 1:** `just check` pass with 0 Svelte diagnostics.
- **Task 1 source criteria:** Confirmed 7 literal white key entries, 5 literal black key entries, separate grid rows, visible row gap, `.pad.white` using `var(--cream)`, `.pad.black` using `var(--black-key)`, and key/chord-name spans present.
- **Task 2:** `just check` pass with 0 Svelte diagnostics.
- **Task 2 source criteria:** Confirmed `onpointerdown`, `onpointerup`, `onpointercancel`, `onlostpointercapture`, `setPointerCapture`, `releasePointerCapture`, `class:held={heldKeys.has(key)}`, and `touch-action: none` remain present.
- **Active-state scan:** Confirmed `var(--accent)`, `var(--accent-soft)`, and the active orange depth value appear only inside the `.pad.held` styling block in `PianoLayout.svelte`.
- **Responsive scan:** Confirmed compact and short-landscape media queries plus label wrapping constraints are present.
- **Overall:** Final `just check` pass.

## Decisions Made

- Restyled the existing component in place to avoid changing the accepted pad DOM and event contract.
- Used existing shared tokens rather than adding local ad hoc colors or spacing.
- Kept visual active state coupled to `class:held={heldKeys.has(key)}` so App.svelte remains the single source of highlight truth.

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0 auto-fixed.
**Impact on plan:** No scope expansion.

## Issues Encountered

None.

## Known Stubs

None.

## Threat Flags

None.

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02.1-04 can browser-smoke desktop, iPad-sized, and iPhone-landscape views against the completed TopBar and pad-surface redesign.
- Browser smoke should specifically verify pointer press/release, keyboard pad highlights, latch highlight sustain/clear, and no orange on non-sounding pads.

## Self-Check: PASSED

- `src/components/PianoLayout.svelte` exists.
- `.planning/phases/02.1-visual-redesign-adoption/02.1-03-SUMMARY.md` exists.
- Task commits found: `16f4aa3`, `65a7682`.
- No generated untracked files remain after verification.

---
*Phase: 02.1-visual-redesign-adoption*
*Completed: 2026-07-06*
