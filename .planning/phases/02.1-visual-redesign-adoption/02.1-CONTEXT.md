# Phase 02.1: Visual Redesign Adoption - Context

**Gathered:** 2026-07-06
**Status:** Ready for planning

<domain>
## Phase Boundary

Adopt the committed Jay-6 visual redesign for the existing v1 feature set before
sequencer work begins.

This phase is a visual adoption pass only:

- Redesign existing app shell, TopBar, pads, variation controls, and retained
  footer/shortcut affordances.
- Preserve current playback, MIDI, latch, clock, keyboard, bank, transpose,
  gate, and variation semantics.
- Do not add progression, sequencer, queued-toast, or other new behavior.

</domain>

<spec_lock>
## Requirements (locked via SPEC.md)

**6 requirements are locked.** See `02.1-SPEC.md` for full requirements,
boundaries, and acceptance criteria.

Downstream agents MUST read `02.1-SPEC.md` before planning or implementing.
Requirements are not duplicated here.

**In scope (from SPEC.md):**

- Adopt visual tokens from the sketch findings for the existing app shell,
  TopBar, pads, variation controls, and footer/shortcut affordance if retained.
- Rework `TopBar.svelte` into the C2 performance-first structure with setup
  popover for existing routing/tempo controls.
- Rework `PianoLayout.svelte` styling into lifted hardware rows while
  preserving the existing pad mapping and press/release contract.
- Replace the raw numeric variation dropdown with per-style controls that map
  to the existing variation indices.
- Preserve existing gate control for Rhythm Gate styles.
- Verify desktop, iPad, and iPhone landscape layouts in browser.

**Out of scope (from SPEC.md):**

- Progression rail display.
- Progression authoring or data mechanism.
- Sequencer UI or behavior.
- Bottom-center queued/countdown toast.
- Hardware OP-1 UAT.
- New playback styles, chord banks, persistence, presets, velocity,
  multi-channel routing, or Web Audio scheduler work.

</spec_lock>

<decisions>
## Implementation Decisions

### Visual Token Adoption

- **D-01: Planner discretion on token structure.** Flo defers to the frontend
  implementer on whether tokens become a small shared CSS layer or stay local to
  the touched components.
- **D-02: Preferred implementation shape.** Use the existing sketch theme as a
  small shared token source where it reduces duplication, then keep component
  styles scoped to the current Svelte components. Do not create a heavy design
  system or add a UI library for this phase.
- **D-03: Semantic color rules are strict.** Orange remains reserved for
  currently sounding or latched pads. Muted steel remains the future queued,
  pending, armed, or next-state accent. Because queued/toast/progression states
  are out of scope, steel may appear only where it supports existing secondary
  system status.

### TopBar Setup Pill And Popover

- **D-04: Use TopBar C2 as the locked direction.** Routing and BPM collapse into
  one performance-first status pill; tapping the pill opens setup controls for
  output, input, channel, clock source, and BPM.
- **D-05: No separate open-popover mock is required.** The sketch package does
  not include an open-popover screenshot. Implement the open state using the same
  visual language as the rest of the redesign: dark instrument chrome, compact
  radii, 8px grid, mono readouts for instrument data, and clear grouping.
- **D-06: Mobile behavior follows responsive usability, not pixel matching an
  absent mock.** iPad uses the two-row stacked TopBar direction from the sketch.
  iPhone landscape must keep the pill and setup controls reachable, avoid
  overlap/clipping, and preserve pad playability. Use browser screenshots and
  smoke checks to validate this.
- **D-07: External clock BPM state is locked.** BPM stays visible in the pill and
  setup surface, but becomes read-only when clock is external.

### Variation Picker Fidelity

- **D-08: Use the full sketch variation language.** Do not ship a simplified raw
  number selector as the planned result.
- **D-09: Arp controls are composed selectors.** Direction, octave range, and
  feel are primary controls; the V-number is derived confirmation.
- **D-10: Beat controls use the grid language.** Note value x straight/triplet
  is the visible model for existing Style 3 variation indices.
- **D-11: Rhythm Gate controls use pattern tiles.** The glyph is the score:
  visible hit/rest/hold notation, not text-first variation numbers.
- **D-12: Mapping must remain exact.** All pickers write through the existing
  `ui.variation` path and map exactly to the current 1..12 variation indices.
  They must not introduce queued application, countdown, or new playback timing.

### Visual Baselines

- **D-13: Treat sketch screenshots as the visual baseline.** The planner should
  use the committed screenshots in `.planning/sketches/001-jay-6-visual-redesign/`
  as the source of visual truth.
- **D-14: Baseline screenshots for this phase.** Use TopBar C2, Arp composed
  selector, full mock v2 recommended, Beat grid, and Rhythm Gate tiles as direct
  references. Use toast and progression screenshots only as future-scope
  references, not implementation targets.
- **D-15: The source HTML remains useful.** The standalone sketch HTML may be
  consulted for spacing, component relationships, and preserved variants, but
  scope comes from `02.1-SPEC.md` and this CONTEXT file.

### The Agent's Discretion

- Choose the exact CSS organization that keeps the implementation readable and
  close to existing Svelte patterns.
- Choose the exact popover dismissal mechanics, focus handling, and responsive
  layout details, provided every existing control remains reachable and the
  browser checks pass.
- Choose whether to extract variation-picker helper components if doing so
  reduces real duplication. Avoid abstraction for its own sake.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Locked Scope

- `.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md` - locked
  requirements, boundaries, acceptance criteria, prohibitions.
- `.planning/PROJECT.md` - core product loop, Web MIDI constraints, active v2
  direction, and load-bearing architecture decisions.
- `.planning/ROADMAP.md` - Phase 02.1 placement before sequencer work.
- `.planning/STATE.md` - current project position and pending todo context.

### Sketch Findings

- `.planning/sketches/MANIFEST.md` - sketch inventory and design direction.
- `.planning/sketches/WRAP-UP-SUMMARY.md` - wrapped visual decisions.
- `.planning/sketches/001-jay-6-visual-redesign/README.md` - winning sketch,
  variants preserved, and scope notes.
- `.planning/sketches/themes/default.css` - extracted theme tokens.
- `.codex/skills/sketch-findings-jay-6/SKILL.md` - packaged design findings.
- `.codex/skills/sketch-findings-jay-6/references/tokens.md` - palette,
  typography, spacing, and semantic accent rules.
- `.codex/skills/sketch-findings-jay-6/references/topbar.md` - TopBar C2
  direction and setup popover contents.
- `.codex/skills/sketch-findings-jay-6/references/pads-and-feedback.md` - lifted
  pad treatment and toast/progression cautions.
- `.codex/skills/sketch-findings-jay-6/references/variations.md` - per-style
  picker language.
- `.codex/skills/sketch-findings-jay-6/references/progressions.md` - future
  progression display direction and explicit implementation caution.

### Visual Baseline Screenshots

- `.planning/sketches/001-jay-6-visual-redesign/01-topbar-c2-recommended.png` -
  TopBar C2 closed state.
- `.planning/sketches/001-jay-6-visual-redesign/02-variations-arp-composed.png` -
  Arp composed variation control.
- `.planning/sketches/001-jay-6-visual-redesign/04-full-mock-v2-recommended.png` -
  overall desktop composition.
- `.planning/sketches/001-jay-6-visual-redesign/05-variations-beat-grid.png` -
  Beat grid variation control.
- `.planning/sketches/001-jay-6-visual-redesign/06-variations-rhythm-gate-tiles.png` -
  Rhythm Gate pattern tiles.

### Future-Scope Visual References

- `.planning/sketches/001-jay-6-visual-redesign/03-progressions-chip-rail.png` -
  future progression rail reference only.
- `.planning/sketches/001-jay-6-visual-redesign/07-toast-bottom-center-pill.png` -
  future queued-toast reference only.

### Codebase Intel

- `.planning/codebase/CONVENTIONS.md` - Svelte state, comments, CSS/code style,
  and data integrity conventions.
- `.planning/codebase/STRUCTURE.md` - component/module layout and where to add
  UI code.
- `.planning/codebase/STACK.md` - Vite/Svelte/Web MIDI/testing stack.

### Implementation Entry Points

- `src/App.svelte` - root shell, keyboard handling, host/tickSource bridge, held
  and latched pad highlight projection.
- `src/components/TopBar.svelte` - main control redesign surface.
- `src/components/PianoLayout.svelte` - pad surface visual redesign surface.
- `src/state.svelte.ts` - existing UI state and style/variation setters.
- `src/phrases.ts` - structured variation metadata for Arp, Beat, and Rhythm
  Gate pickers.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `src/phrases.ts` already exposes structured variation data:
  `ArpVariation`, `PhraseDurationVariation`, and `RhythmVariation`. The
  per-style pickers should derive labels/glyphs from this data instead of
  duplicating variation truth.
- `src/state.svelte.ts` already owns `ui.style`, `ui.variation`,
  `STYLE_LABELS`, and `STYLE_VARIATION_COUNT`. The redesigned pickers should
  continue to use `setStyle()` and `setVariation()`.
- `src/components/PianoLayout.svelte` already has the correct 5-black/7-white
  hardware rows and robust pointer capture/release handling. Treat it as a
  visual restyle, not a behavioral rewrite.
- `.planning/sketches/themes/default.css` and the packaged tokens reference
  provide the source palette and spacing language.

### Established Patterns

- Global UI state lives in `src/state.svelte.ts`; components call setters rather
  than owning app semantics.
- `App.svelte` is the bridge between reactive UI state and imperative
  `EngineHost`/`tickSource` behavior. This phase should not move engine behavior
  into UI components.
- Vitest covers data and math; Svelte/Web MIDI UI behavior is browser-smoked.
- Scoped Svelte CSS is the existing component styling pattern. Shared tokens are
  acceptable when they keep styling consistent without introducing a library.

### Integration Points

- `TopBar.svelte` needs the largest structural change: C2 layout, setup pill,
  popover, per-style variation controls, and responsive stacking.
- `PianoLayout.svelte` needs the lifted bevel pad treatment while preserving key
  mapping, pointer behavior, and held-state class semantics.
- `App.svelte` may need shell/layout styling updates, but should continue to own
  keyboard shortcuts and held/latched highlight projection.
- `state.svelte.ts` should remain the state contract. Add UI-only mapping helpers
  if needed, but avoid changing playback state semantics.

</code_context>

<specifics>
## Specific Ideas

- The UI should read as a compact hardware instrument, not a generic web app or
  SaaS control panel.
- Use the full visual language from the sketch for variation pickers, including
  glyph/notation treatments.
- The missing open-popover screenshot is not a blocker. The open state should be
  designed from the same token/component language and validated in browser.
- Screenshot baselines come from `.planning/sketches/001-jay-6-visual-redesign/`,
  with the SPEC deciding which visual ideas are in scope.

</specifics>

<deferred>
## Deferred Ideas

- Progression rail display and progression authoring remain future milestone
  work.
- Sequencer UI and behavior remain future milestone work.
- Bottom-center queued/countdown toast remains future polish because the current
  app has no queued variation state or countdown behavior.
- Host-owned play/latch single source of truth remains v2 architecture work, not
  part of this visual adoption pass.
- Transport-reset / record-sync remains v2 engine work, not part of this visual
  adoption pass.

</deferred>

---

*Phase: 02.1-visual-redesign-adoption*
*Context gathered: 2026-07-06*
