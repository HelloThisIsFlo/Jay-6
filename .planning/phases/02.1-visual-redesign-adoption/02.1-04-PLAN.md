---
phase: 02.1-visual-redesign-adoption
plan: 04
type: execute
wave: 3
depends_on:
  - 02.1-02
  - 02.1-03
files_modified: []
autonomous: true
requirements:
  - R1-visual-token-adoption
  - R2-topbar-c2-redesign
  - R3-pad-surface-redesign
  - R4-per-style-variation-pickers
  - R5-responsive-redesign-coverage
  - R6-existing-behavior-preservation
must_haves:
  truths:
    - "Desktop browser view matches the v3 sketch direction for tokens, TopBar C2, pad surface, and variation controls per D-13 and D-14."
    - "iPad-sized browser view keeps stacked TopBar/setup controls and all pad rows reachable per D-06."
    - "iPhone-landscape browser view remains scrollable where needed and avoids clipped controls, unreadable primary labels, or inaccessible pad rows per D-06."
    - "Existing controls remain reachable and functional: output, input, channel, clock source, BPM, bank, transpose, style, variation, gate when Rhythm Gate is active, latch, pointer pads, and keyboard pads."
    - "Prohibited surfaces and behavior changes are absent."
  artifacts:
    - path: ".planning/phases/02.1-visual-redesign-adoption/02.1-04-SUMMARY.md"
      provides: "Final verification summary with automated command results and browser smoke notes"
  key_links:
    - from: "src/main.ts"
      to: "src/styles/tokens.css"
      via: "global token import"
      pattern: "styles/tokens.css"
    - from: "src/components/TopBar.svelte"
      to: "src/components/VariationPicker.svelte"
      via: "per-style picker integration"
      pattern: "VariationPicker"
    - from: "src/components/PianoLayout.svelte"
      to: "src/App.svelte"
      via: "heldKeys projection and callbacks"
      pattern: "onPress={press}"
  prohibitions:
    - "No progression rail display, progression authoring, sequencer UI, queued/countdown toast, new playback style, new MIDI behavior, persistence, presets, velocity, multi-channel routing, Web Audio scheduler, or chord-bank data edit."
    - "No orange visual treatment appears for queued, pending, decorative, merely selected, focus, setup, latch-enabled-only, or future-scope states."
---

<objective>
Run the full phase verification pass across source, commands, and browser smoke after all redesign implementation plans complete.

Purpose: Close the visual redesign adoption phase against SPEC acceptance criteria and edge coverage without adding implementation scope.
Output: Verified command results and browser smoke findings recorded in the plan summary.
</objective>

<execution_context>
@/Users/flo/.codex/gsd-core/workflows/execute-plan.md
@/Users/flo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-CONTEXT.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-RESEARCH.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-VALIDATION.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-PATTERNS.md
@.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
@.codex/skills/sketch-findings-jay-6/SKILL.md
@.codex/skills/sketch-findings-jay-6/references/tokens.md
@.codex/skills/sketch-findings-jay-6/references/topbar.md
@.codex/skills/sketch-findings-jay-6/references/pads-and-feedback.md
@.codex/skills/sketch-findings-jay-6/references/variations.md
@src/App.svelte
@src/components/TopBar.svelte
@src/components/PianoLayout.svelte
@src/components/VariationPicker.svelte
@src/variationOptions.ts
@src/state.svelte.ts
@src/phrases.ts
</context>

## Artifacts This Phase Produces

- `src/styles/tokens.css`: shared CSS custom properties for instrument chrome, pad surfaces, typography, radii, spacing, and semantic accents.
- `src/variationOptions.ts`: pure mapping helpers for per-style variation UI models.
- `src/components/VariationPicker.svelte`: per-style variation picker UI.
- `test/variationOptions.test.ts`: exact variation mapping coverage for representative indices.
- Modified `src/main.ts`, `src/App.svelte`, `src/components/TopBar.svelte`, and `src/components/PianoLayout.svelte`.
- Plan summaries: `.planning/phases/02.1-visual-redesign-adoption/02.1-01-SUMMARY.md` through `02.1-04-SUMMARY.md`.

<tasks>

<task type="auto">
  <name>Task 1: Run automated source and regression gates</name>
  <files>none</files>
  <read_first>
    @.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-VALIDATION.md
    @src/main.ts
    @src/App.svelte
    @src/components/TopBar.svelte
    @src/components/PianoLayout.svelte
    @src/components/VariationPicker.svelte
    @src/variationOptions.ts
    @test/variationOptions.test.ts
    @Justfile
  </read_first>
  <action>
    Run the automated verification commands after Plans 01 through 03 are complete. Confirm `just check`, `just test`, and `just ci` all pass. Run a source guard that scans the visual UI surfaces touched by this phase for out-of-scope surface names and fails if any are present there. Confirm `src/banks.data.json`, `src/engines/`, `src/tickSource.ts`, and MIDI timing behavior files were not modified by the redesign unless a prior summary documents a compile-only adapter with rationale.
  </action>
  <verify>
    <automated>just check</automated>
    <automated>just test</automated>
    <automated>just ci</automated>
    <automated>node -e "const fs=require('fs'); const files=['src/App.svelte','src/components/TopBar.svelte','src/components/PianoLayout.svelte','src/components/VariationPicker.svelte','src/styles/tokens.css','src/variationOptions.ts']; const bad=['progression','sequencer','queued','countdown','arms in']; for(const file of files){if(!fs.existsSync(file)) continue; const text=fs.readFileSync(file,'utf8').toLowerCase(); for(const term of bad){if(text.includes(term)){console.error(file+': '+term); process.exit(1);}}}"</automated>
  </verify>
  <acceptance_criteria>
    - `just check`, `just test`, and `just ci` pass.
    - Touched UI surfaces contain no progression, sequencer, queued/countdown, or countdown-toast surface.
    - `src/banks.data.json` is unchanged.
    - No engine, tick source, MIDI timing, or transport file is modified by this visual phase.
  </acceptance_criteria>
  <done>Automated verification proves the redesign builds, tests, and avoids out-of-scope source additions.</done>
</task>

<task type="auto">
  <name>Task 2: Browser smoke desktop, iPad, and iPhone landscape</name>
  <files>none</files>
  <read_first>
    @.planning/phases/02.1-visual-redesign-adoption/02.1-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-UI-SPEC.md
    @.planning/phases/02.1-visual-redesign-adoption/02.1-VALIDATION.md
    @.planning/sketches/001-jay-6-visual-redesign/README.md
    @.codex/skills/sketch-findings-jay-6/references/tokens.md
    @.codex/skills/sketch-findings-jay-6/references/topbar.md
    @.codex/skills/sketch-findings-jay-6/references/pads-and-feedback.md
    @.codex/skills/sketch-findings-jay-6/references/variations.md
  </read_first>
  <action>
    Start the app with `just dev` and perform browser smoke against the completed UI. Use desktop, iPad-sized, and iPhone-landscape viewports. Compare against the required baseline screenshots: `01-topbar-c2-recommended.png`, `02-variations-arp-composed.png`, `04-full-mock-v2-recommended.png`, `05-variations-beat-grid.png`, and `06-variations-rhythm-gate-tiles.png` per D-13 and D-14. Check closed TopBar, open setup popover, external-clock BPM read-only state, bank controls, transpose, latch, Hold no-picker state, Arp picker, Beat grid, Rhythm Gate tiles, Rhythm Gate gate slider, pointer pad press/release, keyboard pad press/release, and latched pad highlight. Confirm all setup controls remain reachable and dismissible across viewports per D-06. Confirm selected variation controls use non-orange treatments and active orange appears only on currently sounding or latched pads per D-03.
  </action>
  <verify>
    <automated>just ci</automated>
    <browser_smoke>Desktop viewport: closed TopBar, setup popover, all variation families, pads, footer, and no prohibited surfaces.</browser_smoke>
    <browser_smoke>iPad-sized viewport: stacked TopBar/setup reachability, 44px custom targets where practical, all pad rows playable.</browser_smoke>
    <browser_smoke>iPhone-landscape viewport: short-viewport scroll escape, no incoherent overlap, no clipped primary labels, all pad rows reachable.</browser_smoke>
  </verify>
  <acceptance_criteria>
    - Desktop view matches the v3 instrument direction for tokens, TopBar C2, pads, and per-style variation controls.
    - iPad-sized view has reachable setup controls and playable pads without overlap.
    - iPhone-landscape view remains usable and scrollable where needed.
    - Pointer and keyboard pad presses light the matching pad and release/latched behavior clears or sustains highlight exactly as before.
    - Variation equivalents for indices `1`, `6`, `7`, and `12` are selectable for Arp, Beat, Rhythm Gate 4, and Rhythm Gate 5 and update through existing `ui.variation`.
    - Output, input, channel, clock source, BPM, bank, transpose, style, variation, gate, and latch controls remain reachable.
  </acceptance_criteria>
  <done>Browser smoke covers all SPEC acceptance criteria and responsive edge coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser controls -> local MIDI app state | Smoke verifies controls still route through existing state and MIDI paths. |
| Visual state -> user interpretation | Smoke verifies orange and selected-state semantics are not misleading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-08 | Tampering | Full redesign source set | medium | mitigate | Task 1 runs `just ci` and source guards; Task 2 verifies browser behavior remains within existing v1 contract. |
| T-02.1-09 | Spoofing | Active/selected visual semantics | low | mitigate | Task 2 checks orange only appears on currently sounding or latched pads. |
| T-02.1-SC | Tampering | npm installs | low | accept | No dependency changes are required; final verification uses existing `just` commands. |
</threat_model>

<verification>
Overall phase checks:
- `just check` passes.
- `just test` passes.
- `just ci` passes.
- Browser smoke passes at desktop, iPad-sized, and iPhone-landscape viewports.
- Source scan confirms prohibited new surfaces are absent.
- `src/banks.data.json`, engine files, tick source, MIDI timing, and transport behavior are unchanged.
</verification>

<success_criteria>
- All six locked SPEC requirements are satisfied.
- All D-01 through D-15 decisions are implemented or respected.
- No deferred idea from CONTEXT.md appears in the runtime UI.
- The redesigned app remains the same v1 instrument behavior with the approved v3 visual treatment.
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-adoption/02.1-04-SUMMARY.md` when done.
</output>
